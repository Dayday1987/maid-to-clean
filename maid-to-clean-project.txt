This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.emergent/emergent.yml
.gitconfig
.gitignore
api/create-checkout-session.js
api/notify-booking.js
api/send-email.js
api/webhook.js
design_guidelines.json
documents/production-checklist
documents/project-build
documents/project-structure.txt
documents/project-task-plan
memory/PRD.md
public/about.html
public/admin/admin-billing.html
public/admin/admin-customers.html
public/admin/admin-dashboard.html
public/admin/admin-messages.html
public/assets/css/admin.css
public/assets/css/dashboard.css
public/assets/css/load-navbar.css
public/assets/css/main.css
public/assets/css/navbar.css
public/assets/css/responsive.css
public/assets/images/bathroom.webp
public/assets/images/commercial-office.webp
public/assets/images/construction-site.webp
public/assets/images/custom-list.webp
public/assets/images/deepc-lean.webp
public/assets/images/desk.webp
public/assets/images/hero-placeholder.jpg
public/assets/images/kitchen.webp
public/assets/images/livingroom.webp
public/assets/images/Look-like-you-need-cleaning.webp
public/assets/images/move-in-out.webp
public/assets/images/Need-cleaning-lady.webp
public/assets/images/new-construction.webp
public/assets/images/new-logo.webp
public/assets/images/profilepic.webp
public/assets/images/small-office.webp
public/assets/images/stairs.webp
public/assets/js/api-config.js
public/assets/js/auth.js
public/assets/js/dashboard-protect.js
public/assets/js/dashboardprotect.js
public/assets/js/load-navbar.js
public/assets/js/main.js
public/components/navbar.html
public/contact.html
public/dashboard/dashboard.html
public/dashboard/payment-success.html
public/dashboard/payments.html
public/dashboard/schedule.html
public/dashboard/settings.html
public/index.html
public/login.html
public/register.html
public/services.html
supabase/schema.sql
supabase/seed.sql
vercel.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path=".gitconfig">
[user]
	email = github@emergent.sh
	name = emergent-agent-e1
</file>

<file path="api/create-checkout-session.js">
// File: /api/create-checkout-session.js

import Stripe from "stripe";
import { createClient } from "@supabase/supabase-js";

const stripe = new Stripe(process.env.STRIPE_SECRET_KEY);

const supabase = createClient(
  process.env.SUPABASE_URL,
  process.env.SUPABASE_SERVICE_ROLE_KEY, // server-only key
);

export default async function handler(req, res) {
  if (req.method !== "POST") {
    return res.status(405).send("Method Not Allowed");
  }

  try {
    const { appointmentId } = req.body;

    if (!appointmentId) {
      return res.status(400).json({ error: "Missing appointmentId" });
    }

    // Fetch appointment
    const { data: appointment, error: appointmentError } = await supabase
      .from("appointments")
      .select("id, quoted_price, user_id, status")
      .eq("id", appointmentId)
      .single();

    if (appointmentError || !appointment) {
      return res.status(404).json({ error: "Appointment not found" });
    }

    // 🔒 SECURITY: Only allow approved appointments
    if (appointment.status !== "approved") {
      return res
        .status(400)
        .json({ error: "Appointment not approved for payment" });
    }

    // Fetch user email
    const { data: user, error: userError } = await supabase
      .from("users")
      .select("email")
      .eq("id", appointment.user_id)
      .single();

    if (userError || !user) {
      return res.status(404).json({ error: "User not found" });
    }

    // Create Stripe session
    const session = await stripe.checkout.sessions.create({
      mode: "payment",
      payment_method_types: ["card"],
      customer_email: user.email,
      line_items: [
        {
          price_data: {
            currency: "usd",
            product_data: {
              name: "Cleaning Service Booking",
            },
            unit_amount: Math.round(appointment.quoted_price * 100),
          },
          quantity: 1,
        },
      ],
      metadata: {
        appointment_id: appointment.id,
        user_id: appointment.user_id,
      },
      success_url: `${process.env.BASE_URL}/dashboard/payment-success.html`,
      cancel_url: `${process.env.BASE_URL}/dashboard/payments.html`,
    });

    return res.status(200).json({ url: session.url });
  } catch (err) {
    console.error("Checkout session error:", err);
    return res.status(500).json({ error: "Internal server error" });
  }
}
</file>

<file path="api/notify-booking.js">
// File: /api/notify-booking.js
// Sends booking confirmation emails to customers and admin

export default async function handler(req, res) {
  if (req.method !== 'POST') {
    return res.status(405).json({ error: 'Method not allowed' });
  }

  const RESEND_API_KEY = process.env.RESEND_API_KEY;
  const SENDER_EMAIL = process.env.SENDER_EMAIL || 'Maid To Clean <onboarding@resend.dev>';
  const ADMIN_EMAIL = process.env.ADMIN_EMAIL || 'stephanie.maidtoclean@gmail.com';

  if (!RESEND_API_KEY) {
    console.error('RESEND_API_KEY not configured');
    return res.status(500).json({ error: 'Email service not configured' });
  }

  try {
    const { 
      customerEmail, 
      customerName, 
      serviceName, 
      appointmentDate, 
      appointmentTime, 
      notes,
      type // 'new_booking', 'approved', 'payment_confirmed'
    } = req.body;

    let customerSubject, customerHtml, adminSubject, adminHtml;

    if (type === 'new_booking') {
      // Email to customer
      customerSubject = 'Booking Request Received - Maid To Clean';
      customerHtml = `
        <!DOCTYPE html>
        <html>
        <head>
          <style>
            body { font-family: 'Segoe UI', Arial, sans-serif; line-height: 1.6; color: #333; }
            .container { max-width: 600px; margin: 0 auto; padding: 20px; }
            .header { background: linear-gradient(135deg, #0F172A 0%, #1e293b 100%); color: white; padding: 30px; text-align: center; border-radius: 8px 8px 0 0; }
            .header h1 { margin: 0; font-size: 24px; }
            .content { background: #f8fafc; padding: 30px; border: 1px solid #e2e8f0; }
            .details { background: white; padding: 20px; border-radius: 8px; margin: 20px 0; }
            .details p { margin: 8px 0; }
            .label { font-weight: 600; color: #0F172A; }
            .accent { color: #06B6D4; }
            .footer { text-align: center; padding: 20px; color: #64748b; font-size: 14px; }
          </style>
        </head>
        <body>
          <div class="container">
            <div class="header">
              <h1>✨ Maid To Clean</h1>
            </div>
            <div class="content">
              <h2>Thank You, ${customerName || 'Valued Customer'}!</h2>
              <p>We've received your booking request and will review it shortly. You'll receive a confirmation email once approved.</p>
              
              <div class="details">
                <p><span class="label">Service:</span> ${serviceName}</p>
                <p><span class="label">Date:</span> ${appointmentDate}</p>
                <p><span class="label">Time:</span> ${appointmentTime}</p>
                ${notes ? `<p><span class="label">Notes:</span> ${notes}</p>` : ''}
              </div>
              
              <p>If you have any questions, feel free to reach out:</p>
              <p>📞 <a href="tel:+13344254566">(334) 425-4566</a></p>
              <p>📧 <a href="mailto:stephanie.maidtoclean@gmail.com">stephanie.maidtoclean@gmail.com</a></p>
            </div>
            <div class="footer">
              <p>Polishing Homes, Empowering Communities, Building Relationships That Last.</p>
              <p>&copy; 2026 Maid To Clean</p>
            </div>
          </div>
        </body>
        </html>
      `;

      // Email to admin
      adminSubject = `🆕 New Booking Request - ${customerName || customerEmail}`;
      adminHtml = `
        <!DOCTYPE html>
        <html>
        <head>
          <style>
            body { font-family: 'Segoe UI', Arial, sans-serif; line-height: 1.6; color: #333; }
            .container { max-width: 600px; margin: 0 auto; padding: 20px; }
            .header { background: #0F172A; color: white; padding: 20px; text-align: center; border-radius: 8px 8px 0 0; }
            .content { background: #f8fafc; padding: 30px; border: 1px solid #e2e8f0; }
            .details { background: white; padding: 20px; border-radius: 8px; margin: 20px 0; border-left: 4px solid #06B6D4; }
            .btn { display: inline-block; background: #06B6D4; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; margin-top: 15px; }
          </style>
        </head>
        <body>
          <div class="container">
            <div class="header">
              <h1>New Booking Request</h1>
            </div>
            <div class="content">
              <p>A new booking request has been submitted:</p>
              
              <div class="details">
                <p><strong>Customer:</strong> ${customerName || 'Not provided'}</p>
                <p><strong>Email:</strong> ${customerEmail}</p>
                <p><strong>Service:</strong> ${serviceName}</p>
                <p><strong>Date:</strong> ${appointmentDate}</p>
                <p><strong>Time:</strong> ${appointmentTime}</p>
                ${notes ? `<p><strong>Notes:</strong> ${notes}</p>` : ''}
              </div>
              
              <a href="https://maid-to-clean.vercel.app/admin/admin-customers.html" class="btn">Review in Admin Panel</a>
            </div>
          </div>
        </body>
        </html>
      `;

    } else if (type === 'approved') {
      customerSubject = '✅ Booking Approved - Maid To Clean';
      customerHtml = `
        <!DOCTYPE html>
        <html>
        <head>
          <style>
            body { font-family: 'Segoe UI', Arial, sans-serif; line-height: 1.6; color: #333; }
            .container { max-width: 600px; margin: 0 auto; padding: 20px; }
            .header { background: linear-gradient(135deg, #10B981 0%, #059669 100%); color: white; padding: 30px; text-align: center; border-radius: 8px 8px 0 0; }
            .content { background: #f8fafc; padding: 30px; border: 1px solid #e2e8f0; }
            .details { background: white; padding: 20px; border-radius: 8px; margin: 20px 0; }
            .btn { display: inline-block; background: linear-gradient(90deg, #06B6D4 0%, #3B82F6 100%); color: white; padding: 14px 28px; text-decoration: none; border-radius: 50px; margin-top: 15px; font-weight: 600; }
            .footer { text-align: center; padding: 20px; color: #64748b; font-size: 14px; }
          </style>
        </head>
        <body>
          <div class="container">
            <div class="header">
              <h1>🎉 Booking Approved!</h1>
            </div>
            <div class="content">
              <h2>Great news, ${customerName || 'Valued Customer'}!</h2>
              <p>Your cleaning appointment has been approved and is ready for payment.</p>
              
              <div class="details">
                <p><strong>Service:</strong> ${serviceName}</p>
                <p><strong>Date:</strong> ${appointmentDate}</p>
                <p><strong>Time:</strong> ${appointmentTime}</p>
              </div>
              
              <p>Please complete your payment to confirm your booking:</p>
              <a href="https://maid-to-clean.vercel.app/dashboard/payments.html" class="btn">Complete Payment</a>
              
              <p style="margin-top: 20px; color: #64748b;">Questions? Call us at (334) 425-4566</p>
            </div>
            <div class="footer">
              <p>&copy; 2026 Maid To Clean</p>
            </div>
          </div>
        </body>
        </html>
      `;
      adminHtml = null; // No admin email needed for approval

    } else if (type === 'payment_confirmed') {
      customerSubject = '💳 Payment Confirmed - Maid To Clean';
      customerHtml = `
        <!DOCTYPE html>
        <html>
        <head>
          <style>
            body { font-family: 'Segoe UI', Arial, sans-serif; line-height: 1.6; color: #333; }
            .container { max-width: 600px; margin: 0 auto; padding: 20px; }
            .header { background: linear-gradient(135deg, #0F172A 0%, #1e293b 100%); color: white; padding: 30px; text-align: center; border-radius: 8px 8px 0 0; }
            .content { background: #f8fafc; padding: 30px; border: 1px solid #e2e8f0; }
            .details { background: white; padding: 20px; border-radius: 8px; margin: 20px 0; border-left: 4px solid #10B981; }
            .footer { text-align: center; padding: 20px; color: #64748b; font-size: 14px; }
          </style>
        </head>
        <body>
          <div class="container">
            <div class="header">
              <h1>✨ Maid To Clean</h1>
            </div>
            <div class="content">
              <h2>Payment Confirmed!</h2>
              <p>Thank you for your payment. Your cleaning service is now scheduled and confirmed.</p>
              
              <div class="details">
                <p><strong>Service:</strong> ${serviceName}</p>
                <p><strong>Date:</strong> ${appointmentDate}</p>
                <p><strong>Time:</strong> ${appointmentTime}</p>
              </div>
              
              <p>We look forward to serving you! Our team will arrive at the scheduled time.</p>
              
              <p style="margin-top: 20px;">Questions? Contact us:</p>
              <p>📞 (334) 425-4566</p>
              <p>📧 stephanie.maidtoclean@gmail.com</p>
            </div>
            <div class="footer">
              <p>Polishing Homes, Empowering Communities, Building Relationships That Last.</p>
              <p>&copy; 2026 Maid To Clean</p>
            </div>
          </div>
        </body>
        </html>
      `;

      // Notify admin of payment
      adminSubject = `💰 Payment Received - ${customerName || customerEmail}`;
      adminHtml = `
        <!DOCTYPE html>
        <html>
        <body style="font-family: Arial, sans-serif; padding: 20px;">
          <h2>Payment Received!</h2>
          <p><strong>Customer:</strong> ${customerName || customerEmail}</p>
          <p><strong>Service:</strong> ${serviceName}</p>
          <p><strong>Date:</strong> ${appointmentDate} at ${appointmentTime}</p>
          <p><a href="https://maid-to-clean.vercel.app/admin/admin-billing.html">View in Admin</a></p>
        </body>
        </html>
      `;
    }

    // Send customer email
    const customerResponse = await fetch('https://api.resend.com/emails', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${RESEND_API_KEY}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        from: SENDER_EMAIL,
        to: [customerEmail],
        subject: customerSubject,
        html: customerHtml
      })
    });

    if (!customerResponse.ok) {
      const error = await customerResponse.json();
      console.error('Customer email failed:', error);
    }

    // Send admin email (if applicable)
    if (adminHtml) {
      const adminResponse = await fetch('https://api.resend.com/emails', {
        method: 'POST',
        headers: {
          'Authorization': `Bearer ${RESEND_API_KEY}`,
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({
          from: SENDER_EMAIL,
          to: [ADMIN_EMAIL],
          subject: adminSubject,
          html: adminHtml
        })
      });

      if (!adminResponse.ok) {
        const error = await adminResponse.json();
        console.error('Admin email failed:', error);
      }
    }

    return res.status(200).json({ success: true, message: 'Notification emails sent' });

  } catch (error) {
    console.error('Notification error:', error);
    return res.status(500).json({ error: 'Failed to send notifications' });
  }
}
</file>

<file path="api/send-email.js">
// File: /api/send-email.js
// Vercel Serverless Function for sending emails via Resend

export default async function handler(req, res) {
  if (req.method !== 'POST') {
    return res.status(405).json({ error: 'Method not allowed' });
  }

  const RESEND_API_KEY = process.env.RESEND_API_KEY;
  const SENDER_EMAIL = process.env.SENDER_EMAIL || 'onboarding@resend.dev';

  if (!RESEND_API_KEY) {
    console.error('RESEND_API_KEY not configured');
    return res.status(500).json({ error: 'Email service not configured' });
  }

  try {
    const { to, subject, html, type } = req.body;

    if (!to || !subject || !html) {
      return res.status(400).json({ error: 'Missing required fields: to, subject, html' });
    }

    const response = await fetch('https://api.resend.com/emails', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${RESEND_API_KEY}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        from: SENDER_EMAIL,
        to: Array.isArray(to) ? to : [to],
        subject: subject,
        html: html
      })
    });

    const data = await response.json();

    if (!response.ok) {
      console.error('Resend API error:', data);
      return res.status(response.status).json({ error: data.message || 'Failed to send email' });
    }

    return res.status(200).json({ 
      success: true, 
      message: 'Email sent successfully',
      id: data.id 
    });

  } catch (error) {
    console.error('Email send error:', error);
    return res.status(500).json({ error: 'Internal server error' });
  }
}
</file>

<file path="design_guidelines.json">
{
  "project_name": "Maid To Clean",
  "design_identity": {
    "persona": "E1 - The Anti-AI Designer",
    "theme": "Clinical Luxury & Futuristic Clean",
    "core_concept": "The Future of Cleanliness. Where precision meets comfort.",
    "emotional_tone": "Trustworthy, Advanced, Pristine, Efficient",
    "archetype": "Archetype 4: Swiss & High-Contrast (with Archetype 7 Accents)"
  },
  "typography": {
    "headings": {
      "font_family": "Syne, sans-serif",
      "import_url": "https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Syne:wght@400;600;700;800&display=swap",
      "usage": "Use 'Syne' for all H1-H6. It provides that unique, futuristic geometric feel.",
      "weights": {
        "h1": "800",
        "h2": "700",
        "h3": "600"
      },
      "letter_spacing": "tracking-tight (-0.02em)"
    },
    "body": {
      "font_family": "Inter, sans-serif",
      "weights": {
        "regular": "400",
        "medium": "500"
      },
      "line_height": "1.6"
    }
  },
  "colors": {
    "palette": {
      "background": "#F8FAFC",
      "surface": "#FFFFFF",
      "primary": "#0F172A",
      "secondary": "#334155",
      "accent": "#06B6D4",
      "accent_glow": "0 0 20px rgba(6, 182, 212, 0.5)",
      "border": "#E2E8F0",
      "success": "#10B981",
      "error": "#EF4444"
    },
    "gradients": {
      "hero_overlay": "linear-gradient(135deg, rgba(15, 23, 42, 0.9) 0%, rgba(15, 23, 42, 0.4) 100%)",
      "button_primary": "linear-gradient(90deg, #06B6D4 0%, #3B82F6 100%)",
      "glass_surface": "rgba(255, 255, 255, 0.7)"
    },
    "rules": {
      "contrast": "Ensure text is #0F172A on light backgrounds and #FFFFFF on dark backgrounds.",
      "forbidden": "Do not use #1B93A4 (Teal) for dashboards. Use #06B6D4 (Cyan) instead."
    }
  },
  "layout_strategies": {
    "grid_system": {
      "type": "CSS Grid",
      "bento_grid": "display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem;",
      "container_padding": "padding: 2rem; max-width: 1280px; margin: 0 auto;"
    },
    "spacing": {
      "scale": "Use 4px base unit. 16px (1rem), 32px (2rem), 64px (4rem), 128px (8rem).",
      "section_spacing": "padding-block: 8rem; (Generous breathing room)"
    },
    "z_index": {
      "modal": 1000,
      "sticky_nav": 900,
      "dropdown": 800,
      "content": 10,
      "background": 1
    }
  },
  "component_guidelines": {
    "buttons": {
      "primary": {
        "css": "background: var(--gradient-primary); color: white; border-radius: 9999px; padding: 1rem 2rem; font-weight: 600; font-family: 'Syne'; transition: transform 0.2s, box-shadow 0.2s;",
        "hover": "transform: translateY(-2px); box-shadow: var(--accent-glow);"
      },
      "secondary": {
        "css": "background: transparent; border: 1px solid var(--border); color: var(--primary); border-radius: 9999px; padding: 1rem 2rem;",
        "hover": "background: var(--surface); border-color: var(--accent);"
      }
    },
    "cards": {
      "glass_panel": {
        "css": "background: rgba(255, 255, 255, 0.7); backdrop-filter: blur(12px); border: 1px solid rgba(255, 255, 255, 0.5); border-radius: 16px; padding: 2rem; box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);"
      },
      "feature_card": {
        "css": "background: white; border: 1px solid var(--border); border-radius: 12px; transition: border-color 0.3s;",
        "hover": "border-color: var(--accent);"
      }
    },
    "inputs": {
      "style": "background: white; border: 1px solid var(--border); padding: 0.75rem 1rem; border-radius: 8px; font-family: 'Inter';",
      "focus": "outline: 2px solid var(--accent); border-color: transparent;"
    }
  },
  "assets": {
    "images": {
      "hero_bg": {
        "url": "https://images.unsplash.com/photo-1725042893312-5ec0dea9e369?crop=entropy&cs=srgb&fm=jpg&ixid=M3w3NDQ2MzR8MHwxfHNlYXJjaHwzfHxtb2Rlcm4lMjBmdXR1cmlzdGljJTIwbGl2aW5nJTIwcm9vbSUyMGNsZWFuJTIwd2hpdGUlMjBicmlnaHQlMjBsdXh1cnklMjBpbnRlcmlvcnxlbnwwfHx8fDE3NzI3Nzc5NDJ8MA&ixlib=rb-4.1.0&q=85",
        "alt": "Futuristic clean living room with round window",
        "description": "Use as full-screen hero background with dark overlay."
      },
      "service_deep_clean": {
        "url": "https://images.unsplash.com/photo-1758273238947-7eb530b408e4?crop=entropy&cs=srgb&fm=jpg&ixid=M3w3NDQ2NDF8MHwxfHNlYXJjaHwyfHxwcm9mZXNzaW9uYWwlMjBob3VzZSUyMGNsZWFuaW5nJTIwc2VydmljZSUyMG1haWQlMjB0ZWFtJTIwd29ya2luZyUyMG1vZGVybnxlbnwwfHx8fDE3NzI3Nzc5NDh8MA&ixlib=rb-4.1.0&q=85",
        "alt": "Deep cleaning service in action",
        "description": "Bright modern room vacuuming."
      },
      "service_move_in": {
        "url": "https://images.unsplash.com/photo-1630699144035-c0f6311ec482?crop=entropy&cs=srgb&fm=jpg&ixid=M3w3NDQ2MzR8MHwxfHNlYXJjaHwyfHxtb2Rlcm4lMjBmdXR1cmlzdGljJTIwbGl2aW5nJTIwcm9vbSUyMGNsZWFuJTIwd2hpdGUlMjBicmlnaHQlMjBsdXh1cnklMjBpbnRlcmlvcnxlbnwwfHx8fDE3NzI3Nzc5NDJ8MA&ixlib=rb-4.1.0&q=85",
        "alt": "Move in/out cleaning - Empty pristine room",
        "description": "Empty room with white rug."
      },
      "team_photo": {
        "url": "https://images.unsplash.com/photo-1758273705792-a2887e079e83?crop=entropy&cs=srgb&fm=jpg&ixid=M3w3NDQ2NDF8MHwxfHNlYXJjaHwzfHxwcm9mZXNzaW9uYWwlMjBob3VzZSUyMGNsZWFuaW5nJTIwc2VydmljZSUyMG1haWQlMjB0ZWFtJTIwd29ya2luZyUyMG1vZGVybnxlbnwwfHx8fDE3NzI3Nzc5NDh8MA&ixlib=rb-4.1.0&q=85",
        "alt": "Professional Maid Team",
        "description": "Cleaner focusing on work."
      }
    },
    "icons": {
      "library": "Phosphor Icons (via CDN)",
      "cdn_link": "<script src=\"https://unpkg.com/@phosphor-icons/web\"></script>",
      "style": "duotone"
    }
  },
  "accessibility": {
    "contrast_ratio": "Ensure 4.5:1 for normal text.",
    "focus_states": "All interactive elements must have a visible focus ring.",
    "aria_labels": "Buttons with icons only must have aria-label."
  },
  "instructions_to_main_agent": [
    "You are working with Vanilla HTML/CSS/JS. DO NOT attempt to import React components.",
    "Use the CDN links provided for Fonts and Icons in the <head> of every HTML file.",
    "Define the CSS Variables in 'assets/css/main.css' :root first.",
    "Implement the 'Glassmorphism' effect for the Navbar and Service Cards.",
    "Use the 'Syne' font for all headings to establish the brand identity.",
    "The Dashboard should use the 'Swiss & High-Contrast' archetype: Stark white backgrounds with bold black text and Cyan accents.",
    "Ensure all images use the 'object-fit: cover' property to maintain aspect ratio.",
    "Add 'data-testid' to all buttons and inputs (e.g., 'login-btn', 'email-input')."
  ],
  "universal_guidelines": [
    "Avoid gradients for text color.",
    "Do not use generic 'SaaS blue' (#3B82F6) as the primary brand color; stick to the defined Deep Space Blue and Cyan accent.",
    "Ensure 80vh max height for hero images on mobile.",
    "Use 'sonner' style toast notifications (implemented via custom JS/CSS since no React)."
  ]
}
</file>

<file path="documents/production-checklist">
You said:
You are a Senior Level web architect and full-stack WebDeveloper.

You are a senior full-stack software engineer.

Your task is to generate a COMPLETE production-ready full-stack website for a cleaning business “Maid To Clean” in one response.

IMPORTANT RULES:

- Do NOT guess business details.
- Do NOT invent content.
- Do NOT mention anything about being licensed or insured.
- Use clearly labeled placeholders with comments wherever information is needed.
- Use image placeholders where I can paste my own image URLs.
- Organize everything cleanly for scalability.
- Separate all files properly.
- Follow mobile-first responsive design principles.
- Use clean, modern UI.
- Use best practices for performance, accessibility, and SEO.

==================================================
TECH STACK
==================================================

Frontend:

- HTML5
- CSS3 (separate files, organized by component/page)
- Vanilla JavaScript (modular structure)

Backend / Services:

- Supabase (database + auth)
- Stripe (payments)
- Vercel (deployment ready)
- GitHub-ready project structure

==================================================
PROJECT STRUCTURE
==================================================

Generate full folder/file structure like this:

/public
index.html
about.html
services.html
contact.html
login.html
register.html
/dashboard
dashboard.html
schedule.html
payments.html
payment-success.html
settings.html
/admin
admin-dashboard.html
admin-messages.html
admin-customers.html
admin-billing.html

/assets
/css
main.css
navbar.css
dashboard.css
admin.css
responsive.css
/js
api-config.js
auth.js
dashboard-protect.js
load-navbar.js
main.js
/images
(use placeholders only)

/components
navbar.html
footer.html

==================================================
PUBLIC WEBSITE REQUIREMENTS
==================================================

Create either:

OPTION A:

- One Landing Page (index.html) with:
  - Universal Navbar
  - Hero Section (Title, Slogan, CTA)
  - About Section
  - Services Section
  - Trusted Section
  - Why Us Section
  - Contact Section
  - Register CTA section
  - Footer (links + copyright placeholder)

OR

OPTION B:
Separate Pages:

1. Homepage (intro + CTA "Book Today" → Call or Register)
2. About
3. Services
4. Contact
5. Login
6. Register

Use section-related transparent background images on homepage.
Use placeholders like:

<!-- IMAGE PLACEHOLDER: hero-bg -->
<img src="PASTE_IMAGE_URL_HERE" alt="Description">

Use content placeholders like:

<!-- BUSINESS_NAME -->
<!-- PHONE_NUMBER -->
<!-- EMAIL -->
<!-- SERVICE_LIST -->
<!-- ABOUT_TEXT -->
<!-- WHY_CHOOSE_US_POINTS -->

CTAs should appear naturally throughout but not excessively.

==================================================
DASHBOARD (CUSTOMER ROLE)
==================================================

1. Dashboard
   - Welcome message (dynamic from Supabase user)
   - Admin message box
   - Last scheduled service box
   - Next scheduled service box
   - Overview stats

2. Schedule Page
   - View past schedules
   - Create new schedule
   - Real-time available date/time slots
   - Auto-update service calendar
   - Service type dropdown
   - Add-ons dropdown
   - Notes field
   - Payment type dropdown with:
     - Card
     - Apple Pay
     - PayPal
     - Cash
   - Each payment type in its own dropdown section

3. Payments Page
   - Transaction history
   - Saved cards
   - Scheduled payments
   - Balance display
   - Payment options

4. Payment Success Page

5. Settings
   - Edit profile
   - Delete account
   - Upload profile image
   - Contact support

==================================================
ADMIN PANEL
==================================================

1. Admin Dashboard
   - Quick overview stats
   - Last global message
   - New inbox count

2. Admin Messages
   - Message history
   - Edit welcome message
   - Send private customer message
   - Receive booking requests

3. Admin Customers
   - List all customers
   - View profile
   - Edit accounts

4. Admin Billing
   - Customer summaries
   - Business summary
   - Invoices
   - Billing history
   - Computed totals

==================================================
JAVASCRIPT REQUIREMENTS
==================================================

api-config.js

- Supabase initialization
- Stripe initialization
- Environment variable placeholders

auth.js

- Register
- Login
- Logout
- Session persistence
- Role detection (admin/customer)

dashboard-protect.js

- Route protection
- Role-based access control

load-navbar.js

- Dynamically load navbar
- Show/hide links based on user role

main.js

- General UI interactions
- Form validation
- Schedule creation logic
- Calendar auto-update logic

==================================================
NAVBAR
==================================================

Universal navbar:

- Dynamic links based on role:
  - Guest
  - Customer
  - Admin
- Mobile hamburger
- Sticky
- Clean modern style

==================================================
STRIPE INTEGRATION
==================================================

- Checkout session creation
- Webhook placeholder
- Payment confirmation flow
- Payment success redirect
- Saved payment methods

Use comments for:

<!-- STRIPE_SECRET_KEY -->
<!-- SUPABASE_URL -->
<!-- SUPABASE_ANON_KEY -->

==================================================
DATABASE STRUCTURE (SUPABASE)
==================================================

Generate SQL schema for:

- users (role column required)
- services
- appointments
- payments
- messages
- add-ons

Include relationships and indexes.

==================================================
STYLING
==================================================

- Mobile-first
- Optimized
- Separated CSS
- Future scalable structure
- Reusable utility classes
- Clean professional design
- Subtle animations
- Modern UI

==================================================
DELIVERABLE FORMAT
==================================================

1. Show full folder structure
2. Then output every file clearly labeled like:

// File: public/index.html
<code here>

// File: assets/css/main.css
<code here>

3. Include all necessary HTML, CSS, JS, and SQL.
4. Use placeholders instead of guessing content.
5. Ensure it is deploy-ready for Vercel.

Generate everything now.

Code said:
Below is a complete, production-ready full-stack structure for “Maid To Clean” built with:

HTML5

Modular CSS

Vanilla JS (modular)

Supabase (DB + Auth)

Stripe (Payments)

Vercel-ready

GitHub-ready structure

Mobile-first

Scalable architecture

No guessed business details

All placeholders clearly labeled

✅ FULL PROJECT STRUCTURE
/public
index.html
about.html
services.html
contact.html
login.html
register.html
/dashboard
dashboard.html
schedule.html
payments.html
payment-success.html
settings.html
/admin
admin-dashboard.html
admin-messages.html
admin-customers.html
admin-billing.html

/assets
/css
main.css
navbar.css
dashboard.css
admin.css
responsive.css
/js
api-config.js
auth.js
dashboard-protect.js
load-navbar.js
main.js
/images
hero-placeholder.jpg
about-placeholder.jpg
service-placeholder.jpg

/components
navbar.html
footer.html

/supabase
schema.sql

# /vercel.json

# PUBLIC WEBSITE

// File: public/index.html

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="<!-- BUSINESS_NAME --> Professional cleaning services." />
  <title><!-- BUSINESS_NAME --> | Professional Cleaning</title>

  <link rel="stylesheet" href="/assets/css/main.css" />
  <link rel="stylesheet" href="/assets/css/navbar.css" />
  <link rel="stylesheet" href="/assets/css/responsive.css" />

  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
  <script defer src="/assets/js/main.js"></script>
</head>

<body>

<div id="navbar"></div>

<section class="hero">
  <!-- IMAGE PLACEHOLDER: hero-bg -->
  <img src="/assets/images/hero-placeholder.jpg" alt="Cleaning Service" class="hero-bg">

  <div class="hero-content">
    <h1><!-- BUSINESS_NAME --></h1>
    <p><!-- SLOGAN_PLACEHOLDER --></p>
    <a href="/register.html" class="btn-primary">Book Today</a>
  </div>
</section>

<section class="about">
  <h2>About Us</h2>
  <p><!-- ABOUT_TEXT --></p>
  <img src="/assets/images/about-placeholder.jpg" alt="About cleaning team">
</section>

<section class="services-preview">
  <h2>Our Services</h2>
  <ul>
    <!-- SERVICE_LIST -->
  </ul>
  <a href="/services.html" class="btn-secondary">View All Services</a>
</section>

<section class="why-us">
  <h2>Why Choose Us</h2>
  <ul>
    <!-- WHY_CHOOSE_US_POINTS -->
  </ul>
</section>

<section class="contact-cta">
  <h2>Ready to Schedule?</h2>
  <p>Call: <!-- PHONE_NUMBER --></p>
  <p>Email: <!-- EMAIL --></p>
  <a href="/register.html" class="btn-primary">Create Account</a>
</section>

<div id="footer"></div>

</body>
</html>
==============================
NAVBAR COMPONENT
==============================
// File: components/navbar.html
<nav class="navbar">
  <div class="nav-container">
    <a href="/index.html" class="logo"><!-- BUSINESS_NAME --></a>

    <button class="hamburger" id="hamburger">&#9776;</button>

    <ul class="nav-links" id="nav-links">
      <li><a href="/about.html">About</a></li>
      <li><a href="/services.html">Services</a></li>
      <li><a href="/contact.html">Contact</a></li>

      <li class="guest-only"><a href="/login.html">Login</a></li>
      <li class="guest-only"><a href="/register.html">Register</a></li>

      <li class="customer-only"><a href="/dashboard/dashboard.html">Dashboard</a></li>

      <li class="admin-only"><a href="/admin/admin-dashboard.html">Admin</a></li>

      <li class="auth-only"><a href="#" id="logoutBtn">Logout</a></li>
    </ul>

  </div>
</nav>
==============================
SUPABASE + STRIPE CONFIG
==============================
// File: assets/js/api-config.js
// SUPABASE CONFIGURATION
// Replace with your environment variables

const SUPABASE_URL = "YOUR_SUPABASE_URL"; // <!-- SUPABASE_URL -->
const SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY"; // <!-- SUPABASE_ANON_KEY -->

const supabase = window.supabase.createClient(
SUPABASE_URL,
SUPABASE_ANON_KEY
);

// STRIPE CONFIGURATION
const STRIPE_PUBLIC_KEY = "YOUR_STRIPE_PUBLIC_KEY"; // <!-- STRIPE_SECRET_KEY -->
const stripe = Stripe(STRIPE_PUBLIC_KEY);
==============================
AUTH SYSTEM
==============================
// File: assets/js/auth.js
async function registerUser(email, password) {
const { data, error } = await supabase.auth.signUp({
email,
password
});

if (error) throw error;

await supabase.from('users').insert([
{
id: data.user.id,
role: 'customer'
}
]);
}

async function loginUser(email, password) {
const { error } = await supabase.auth.signInWithPassword({
email,
password
});

if (error) throw error;
}

async function logoutUser() {
await supabase.auth.signOut();
window.location.href = "/index.html";
}

async function getCurrentUser() {
const { data } = await supabase.auth.getUser();
return data.user;
}

async function getUserRole() {
const user = await getCurrentUser();
if (!user) return null;

const { data } = await supabase
.from("users")
.select("role")
.eq("id", user.id)
.single();

return data?.role;
}
==============================
DASHBOARD PROTECTION
==============================
// File: assets/js/dashboard-protect.js
document.addEventListener("DOMContentLoaded", async () => {
const user = await getCurrentUser();

if (!user) {
window.location.href = "/login.html";
return;
}

const role = await getUserRole();

if (window.location.pathname.includes("/admin") && role !== "admin") {
window.location.href = "/dashboard/dashboard.html";
}
});
==============================
SUPABASE DATABASE SCHEMA
==============================
// File: supabase/schema.sql
create table users (
id uuid primary key references auth.users(id) on delete cascade,
role text not null check (role in ('admin', 'customer')),
created_at timestamp default now()
);

create table services (
id uuid primary key default gen_random_uuid(),
name text not null,
description text,
base_price numeric not null,
created_at timestamp default now()
);

create table add_ons (
id uuid primary key default gen_random_uuid(),
name text not null,
price numeric not null
);

create table appointments (
id uuid primary key default gen_random_uuid(),
user_id uuid references users(id),
service_id uuid references services(id),
appointment_date date not null,
appointment_time time not null,
notes text,
status text default 'pending',
created_at timestamp default now()
);

create index idx_appointments_user on appointments(user_id);

create table payments (
id uuid primary key default gen_random_uuid(),
user_id uuid references users(id),
appointment_id uuid references appointments(id),
amount numeric not null,
status text,
stripe_session_id text,
created_at timestamp default now()
);

create table messages (
id uuid primary key default gen_random_uuid(),
sender_id uuid references users(id),
receiver_id uuid references users(id),
content text,
created_at timestamp default now()
);
==============================
MAIN CSS (Mobile First)
==============================
// File: assets/css/main.css
:root {
--primary: #1e3a8a;
--secondary: #f1f5f9;
--accent: #2563eb;
--text: #1f2937;
}

- {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  }

body {
font-family: system-ui, sans-serif;
color: var(--text);
background: #fff;
line-height: 1.6;
}

section {
padding: 3rem 1.5rem;
}

.hero {
position: relative;
text-align: center;
color: white;
}

.hero-bg {
width: 100%;
height: 70vh;
object-fit: cover;
filter: brightness(0.6);
}

.hero-content {
position: absolute;
top: 40%;
left: 50%;
transform: translate(-50%, -50%);
}

.btn-primary {
background: var(--accent);
color: white;
padding: 0.75rem 1.5rem;
text-decoration: none;
display: inline-block;
margin-top: 1rem;
border-radius: 6px;
transition: 0.3s;
}

.btn-primary:hover {
opacity: 0.9;
}
==============================
VERCEL CONFIG
==============================
// File: vercel.json
{
"rewrites": [
{ "source": "/(.*)", "destination": "/public/$1" }
]
}
==============================
STRIPE CHECKOUT PLACEHOLDER (SERVERLESS)
==============================
Create /api/create-checkout-session.js for Vercel serverless:

// This file goes in /api for Vercel

import Stripe from "stripe";

const stripe = new Stripe(process.env.STRIPE_SECRET_KEY);

export default async function handler(req, res) {
const session = await stripe.checkout.sessions.create({
payment_method_types: ['card'],
mode: 'payment',
line_items: [
{
price_data: {
currency: 'usd',
product_data: {
name: 'Cleaning Service'
},
unit_amount: 1000
},
quantity: 1
}
],
success_url: `${req.headers.origin}/dashboard/payment-success.html`,
cancel_url: `${req.headers.origin}/dashboard/payments.html`
});

res.json({ id: session.id });
}
</file>

<file path="documents/project-build">
============================
WHAT THIS PROJECT INCLUDES
============================
Public marketing website
Role-based authentication
Customer dashboard
Admin panel
Supabase schema
Stripe integration
Mobile-first modern UI
Clean scalable structure
Vercel deployment ready
GitHub-ready

=============================
BUILT WITH
=============================
HTML5
Modular CSS
Vanilla JS (modular)
Supabase (DB + Auth)
Stripe (Payments)
Vercel-ready
GitHub-ready structure
Mobile-first
Scalable architecture
</file>

<file path="documents/project-structure.txt">
/public
   index.html
   about.html
   services.html
   contact.html
   login.html
   register.html
   /dashboard
        dashboard.html
        schedule.html
        payments.html
        payment-success.html
        settings.html
   /admin
        admin-dashboard.html
        admin-messages.html
        admin-customers.html
        admin-billing.html

/assets
   /css
       main.css
       navbar.css
       dashboard.css
       admin.css
       responsive.css
   /js
       api-config.js
       auth.js
       dashboard-protect.js
       load-navbar.js
       main.js
   /images
       hero-placeholder.jpg
       about-placeholder.jpg
       service-placeholder.jpg

/components
   navbar.html
   footer.html

/supabase
   schema.sql

/vercel.json

==== 2/28/2026 ====
✅ SYSTEM STATUS 

This system now includes:

✔ Complete dashboard
✔ Complete admin panel
✔ Real-time scheduling
✔ Slot locking
✔ Stripe webhook verification
✔ Database schema
✔ Seed data
✔ Role protection
✔ Production structure
</file>

<file path="documents/project-task-plan">
=======================
Phase 1 — Project Setup
=======================

1. # Initialize Repository
   Create GitHub repository: maid-to-clean

Clone locally

Add provided folder structure

Add .gitignore

Add README.md

Commit initial scaffold

2. # Environment Setup
   Create Supabase project

Create Stripe account

Create Vercel account

Set up environment variables (local + Vercel)

==========================================
🧱 Phase 2 — Database Setup (Supabase)
Tasks
==========================================
Open Supabase SQL Editor

Paste supabase/schema.sql

Run migration

Enable Row Level Security (RLS)

Add policies

Required RLS Policies
Users Table

Users can read their own row

Admin can read all users

Appointments

Users can create appointments

Users can read their own appointments

Admin can read all appointments

Payments

Users can read their own payments

Admin can read all payments

Messages

Users can read their own messages

Admin can read all messages

=============================
🔐 Phase 3 — Authentication
Tasks
=============================
Test registration flow

Verify role default = customer

Manually insert one admin user in database

Verify login

Verify logout

Test role-based routing

Test cases:

Guest → Dashboard = Redirect to login

Customer → Admin page = Blocked

Admin → Admin panel = Accessible

=================================
💳 Phase 4 — Stripe Integration
Tasks
=================================
Create Stripe product (placeholder)

Configure webhook endpoint

Add Vercel environment variables:

STRIPE_SECRET_KEY

STRIPE_WEBHOOK_SECRET

Test checkout session

Verify payment success redirect

Store session ID in Supabase

Confirm payment status update

Test scenarios:

Successful payment

Cancelled payment

Duplicate payment prevention

=============================
📅 Phase 5 — Scheduling Logic
Tasks
=============================
Build available slot query

Prevent double booking

Add add-on pricing logic

Store notes

Test booking flow

Verify admin sees booking

============================
👩‍💼 Phase 6 — Admin Panel
Tasks
============================
Dashboard overview stats query

Customer list view

Edit customer role

Billing summary computation

Message broadcast logic

===============================
🎨 Phase 7 — UI & Optimization
Tasks
===============================
Mobile responsiveness testing

Lighthouse performance audit

Optimize images

Add loading states

Add empty states

Improve accessibility (ARIA labels)

Add meta tags for SEO

================================
🧪 Phase 8 — Testing Checklist
Functional Testing
================================
Register

Login

Logout

Role detection

Create appointment

Pay with Stripe

View dashboard

Admin views data

Delete account

Security Testing

RLS prevents cross-user access

Admin-only routes protected

Stripe keys not exposed

Webhook secret verified

======================
🚀 DEPLOYMENT GUIDE
======================
======================
1️⃣ SUPABASE SETUP
======================
Step 1 — Create Project
=======================
Go to https://supabase.com

Click "New Project"

Choose region

Save:

Project URL

Anon public key

# Step 2 — Run Schema

Open SQL Editor

Paste schema.sql

Execute

# Step 3 — Enable Email Auth

Authentication → Providers

Enable Email

# Step 4 — Add Admin User

After registering:

update users
set role = 'admin'
where id = 'USER_UUID_HERE';

==============================
2️⃣ STRIPE SETUP
=============================
Step 1 — Create Product
==============================
Stripe Dashboard → Products

Add placeholder product

# Step 2 — Get Keys

Developers → API Keys

Copy:

Publishable Key

Secret Key

# Step 3 — Webhook Setup

Endpoint:

https://your-domain.vercel.app/api/webhook

Events to listen for:

checkout.session.completed

Copy:

Webhook signing secret

======================
3️⃣ VERCEL DEPLOYMENT
======================
Step 1 — Install Vercel CLI (Optional)
======================================
npm i -g vercel

# Step 2 — Push to GitHub

git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin YOUR_REPO_URL
git push -u origin main

# Step 3 — Import to Vercel

Go to https://vercel.com

Import GitHub repo

Framework preset: Other

Root directory: leave default

# Step 4 — Add Environment Variables (IMPORTANT)

In Vercel Project → Settings → Environment Variables:

SUPABASE_URL
SUPABASE_ANON_KEY
STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET
STRIPE_PUBLIC_KEY

Deploy.

# 4️⃣ DOMAIN CONFIGURATION (Optional)

Go to Vercel → Domains

Add custom domain

Update DNS records

=====================================
🔄 CONTINUOUS DEPLOYMENT WORKFLOW
Production Branch Strategy
main → production
dev → development
feature/\* → feature branches
Workflow
=====================================
Create feature branch

Push

Pull request

Merge into main

Auto-deploy triggered by Vercel

========================================
📈 PERFORMANCE OPTIMIZATION CHECKLIST
=======================================
Enable Supabase indexing

Use lazy loading for images

Minify CSS/JS

Enable HTTP caching

Use Vercel Edge caching

Compress images (WebP preferred)

Use CDN (Vercel automatic)

================================
🔒 SECURITY BEST PRACTICES
================================
Never expose Stripe secret key in frontend

Use serverless functions for payment creation

Enable RLS on all tables

Use HTTPS (Vercel auto)

Validate webhook signatures

Rate-limit API endpoints (if scaling)

====================================
📊 MONITORING & MAINTENANCE
Supabase
====================================
Monitor database usage

Monitor API logs

Stripe

Monitor failed payments

Monitor disputes

Vercel

Monitor function logs

Monitor performance

==========================
🧾 OPTIONAL IMPROVEMENTS
==========================
Add email notifications

Add SMS reminders

Add service area filtering

Add subscription recurring billing

Add referral system

Add analytics dashboard

Add service reviews

Add staff assignment system

===========================
🗂 DEPLOYMENT FLOW SUMMARY
Local Dev
↓
GitHub
↓
Vercel Auto Deploy
↓
Supabase (DB + Auth)
↓
Stripe (Payments)
===========================
</file>

<file path="public/assets/css/load-navbar.css">
/* Reserved for navbar dynamic states */
.guest-only {
  display: block; /* visible by default */
}

.auth-only {
  display: none; /* hidden until logged in */
}

.customer-only {
  display: none;
}

.admin-only {
  display: none;
}
</file>

<file path="public/assets/js/dashboard-protect.js">
// ================================
// DASHBOARD PROTECTION
// Route guard for protected pages
// ================================

document.addEventListener("DOMContentLoaded", async () => {
  const user = await getCurrentUser();
  const currentPath = window.location.pathname;

  // If no user is logged in, redirect to login
  if (!user) {
    window.location.href = "/login.html";
    return;
  }

  // Get user role
  const role = await getUserRole();

  // Admin page protection
  if (currentPath.includes("/admin") && role !== "admin") {
    alert("Access denied. Admin privileges required.");
    window.location.href = "/dashboard/dashboard.html";
    return;
  }

  // Customer trying to access admin
  if (currentPath.includes("/admin") && role === "customer") {
    window.location.href = "/dashboard/dashboard.html";
    return;
  }
});

// Helper to check if user is authenticated
async function requireAuth() {
  const user = await getCurrentUser();
  if (!user) {
    window.location.href = "/login.html";
    return false;
  }
  return true;
}

// Helper to check if user is admin
async function requireAdmin() {
  const user = await getCurrentUser();
  if (!user) {
    window.location.href = "/login.html";
    return false;
  }

  const role = await getUserRole();
  if (role !== "admin") {
    window.location.href = "/dashboard/dashboard.html";
    return false;
  }
  return true;
}
</file>

<file path="public/assets/js/dashboardprotect.js">
document.addEventListener("DOMContentLoaded", async () => {
  const user = await getCurrentUser();

  if (!user) {
    window.location.href = "/login.html";
    return;
  }

  const role = await getUserRole();

  if (window.location.pathname.includes("/admin") && role !== "admin") {
    window.location.href = "/dashboard/dashboard.html";
  }
});
</file>

<file path="public/assets/js/main.js">
// ==============================
// MAIN.JS - Residential Booking
// ==============================

document.addEventListener("DOMContentLoaded", () => {
  const serviceSelect = document.getElementById("serviceSelect");
  const roomsSelect = document.getElementById("roomsSelect");
  const squareFootageInput = document.getElementById("squareFootage");
  const addonSelect = document.getElementById("addonSelect");
  const customOptions = document.getElementById("customOptions");
  const appointmentDate = document.getElementById("appointmentDate");
  const timeSlots = document.getElementById("timeSlots");
  const notesInput = document.getElementById("notes");
  const scheduleForm = document.getElementById("scheduleForm");

  // ==============================
  // 1️⃣ Load Services Dynamically
  // ==============================
  async function loadServices() {
    const { data: services, error } = await supabase
      .from("services")
      .select("*")
      .order("created_at", { ascending: true });

    if (error) return console.error("Error loading services:", error);

    services.forEach((s) => {
      const option = document.createElement("option");
      option.value = s.id;
      option.textContent = s.name;
      serviceSelect.appendChild(option);
    });
  }

  // ==============================
  // 2️⃣ Load Add-Ons Dynamically
  // ==============================
  async function loadAddOns() {
    const { data: addOns, error } = await supabase.from("add_ons").select("*");

    if (error) return console.error("Error loading add-ons:", error);

    addOns.forEach((a) => {
      const option = document.createElement("option");
      option.value = a.id;
      option.textContent = a.name;
      addonSelect.appendChild(option);
    });
  }

  // ==============================
  // 3️⃣ Show Custom Cleaning Options Conditionally
  // ==============================
  serviceSelect.addEventListener("change", (e) => {
    const selectedText =
      serviceSelect.options[serviceSelect.selectedIndex].text;
    if (selectedText === "Custom Cleaning") {
      customOptions.style.display = "block";
    } else {
      customOptions.style.display = "none";
    }
  });

  // ==============================
  // 4️⃣ Load Available Time Slots (Real-Time Slot Locking)
  // ==============================
  async function loadTimeSlots(date) {
    timeSlots.innerHTML = "";

    if (!date) return;

    // Define possible slots
    const slots = ["08:00", "10:00", "12:00", "14:00", "16:00", "18:00"];

    // Fetch existing appointments for the date
    const { data: booked, error } = await supabase
      .from("appointments")
      .select("appointment_time")
      .eq("appointment_date", date);

    if (error) return console.error(error);

    const bookedTimes = booked.map((b) => b.appointment_time);

    slots.forEach((s) => {
      if (!bookedTimes.includes(s)) {
        const option = document.createElement("option");
        option.value = s;
        option.textContent = s;
        timeSlots.appendChild(option);
      }
    });
  }

  appointmentDate.addEventListener("change", (e) => {
    loadTimeSlots(e.target.value);
  });

  // ==============================
  // 5️⃣ Submit Booking Request
  // ==============================
  scheduleForm.addEventListener("submit", async (e) => {
    e.preventDefault();

    const user = supabase.auth.user();
    if (!user) return alert("You must be logged in to book.");

    const serviceId = serviceSelect.value;
    const rooms = parseInt(roomsSelect.value);
    const squareFootage = parseInt(squareFootageInput.value) || null;
    const appointment_date = appointmentDate.value;
    const appointment_time = timeSlots.value;
    const notes = notesInput.value;

    // Collect selected add-ons
    const selectedAddOns = Array.from(addonSelect.selectedOptions).map(
      (opt) => opt.value,
    );

    // Collect custom cleaning options if visible
    let customDetails = null;
    if (customOptions.style.display === "block") {
      customDetails = {};

      // Rooms checkboxes
      const roomChecks = customOptions.querySelectorAll("input[name='rooms']");
      customDetails.rooms = Array.from(roomChecks)
        .filter((cb) => cb.checked)
        .map((cb) => cb.value);

      // Appliances
      const applianceChecks = customOptions.querySelectorAll(
        "input[name='appliances']",
      );
      const allAppliances =
        customOptions.querySelector("#allAppliances").checked;
      customDetails.appliances = allAppliances
        ? "all"
        : Array.from(applianceChecks)
            .filter((cb) => cb.checked)
            .map((cb) => cb.value);

      // Windows, baseboards, laundry
      customDetails.windows = customOptions.querySelector(
        "input[name='windows']",
      ).checked
        ? "all"
        : null;
      customDetails.baseboards = customOptions.querySelector(
        "input[name='baseboards']",
      ).checked;
      customDetails.laundry = customOptions.querySelector(
        "input[name='laundry']",
      ).checked;
    }

    // Insert appointment into Supabase
    const { data, error } = await supabase.from("appointments").insert([
      {
        user_id: user.id,
        service_id: serviceId,
        rooms: rooms,
        square_footage: squareFootage,
        appointment_date,
        appointment_time,
        notes,
        custom_details: customDetails,
        status: "pending",
      },
    ]);

    if (error) return alert("Error submitting booking: " + error.message);

    alert("Booking request submitted! Admin will review and approve.");

    // Reset form
    scheduleForm.reset();
    customOptions.style.display = "none";
    timeSlots.innerHTML = "";
  });

  // ==============================
  // 6️⃣ Initial Load
  // ==============================
  loadServices();
  loadAddOns();
});
</file>

<file path="vercel.json">
{
  "rewrites": [{ "source": "/(.*)", "destination": "/public/$1" }]
}
</file>

<file path="api/webhook.js">
// File: /api/stripe-webhook.js

import Stripe from "stripe";
import { buffer } from "micro";
import { createClient } from "@supabase/supabase-js";

export const config = {
  api: {
    bodyParser: false,
  },
};

const stripe = new Stripe(process.env.STRIPE_SECRET_KEY);

const supabase = createClient(
  process.env.SUPABASE_URL,
  process.env.SUPABASE_SERVICE_ROLE_KEY, // IMPORTANT: service role key
);

export default async function handler(req, res) {
  if (req.method !== "POST") {
    return res.status(405).send("Method Not Allowed");
  }

  const sig = req.headers["stripe-signature"];
  const rawBody = await buffer(req);

  let event;

  try {
    event = stripe.webhooks.constructEvent(
      rawBody,
      sig,
      process.env.STRIPE_WEBHOOK_SECRET,
    );
  } catch (err) {
    console.error("Webhook signature verification failed:", err.message);
    return res.status(400).send(`Webhook Error: ${err.message}`);
  }

  if (event.type === "checkout.session.completed") {
    const session = event.data.object;

    const appointmentId = session.metadata.appointment_id;
    const userId = session.metadata.user_id;

    try {
      // Get appointment and user details for email
      const { data: appointment } = await supabase
        .from("appointments")
        .select("*, services (name), users (email, full_name)")
        .eq("id", appointmentId)
        .single();

      // Update appointment
      await supabase
        .from("appointments")
        .update({ status: "paid" })
        .eq("id", appointmentId);

      // Insert payment record
      await supabase.from("payments").insert([
        {
          user_id: userId,
          appointment_id: appointmentId,
          amount: session.amount_total / 100,
          status: "paid",
          stripe_session_id: session.id,
        },
      ]);

      // Send payment confirmation email
      if (appointment) {
        try {
          await fetch(`${process.env.BASE_URL}/api/notify-booking`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              customerEmail: appointment.users?.email || session.customer_email,
              customerName: appointment.users?.full_name || '',
              serviceName: appointment.services?.name || 'Cleaning Service',
              appointmentDate: new Date(appointment.appointment_date).toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric', year: 'numeric' }),
              appointmentTime: appointment.appointment_time,
              type: 'payment_confirmed'
            })
          });
        } catch (emailErr) {
          console.error('Payment confirmation email failed:', emailErr);
        }
      }

      console.log("Payment recorded successfully");
    } catch (err) {
      console.error("Database update error:", err);
    }
  }

  res.status(200).json({ received: true });
}
</file>

<file path="memory/PRD.md">
# Maid To Clean - Product Requirements Document

## Original Problem Statement
Finish building a cleaning service website "Maid To Clean" deployed through Vercel with Supabase and Stripe integration. User requested futuristic design, service cards grid, CTAs throughout, and specific About page content. Added email notifications for bookings.

## Architecture
- **Frontend**: Vanilla HTML5, CSS3, JavaScript (static site)
- **Backend**: Supabase (PostgreSQL + Auth)
- **Payments**: Stripe Checkout
- **Email**: Resend (transactional emails)
- **Deployment**: Vercel (static hosting + serverless functions)
- **Icons**: Phosphor Icons
- **Fonts**: Syne (headings) + Inter (body)

## User Personas
1. **Customers**: Homeowners/businesses seeking cleaning services
2. **Admin**: Business owner (stephanie.maidtoclean@gmail.com) managing bookings, customers, billing

## Core Requirements
- [x] Public marketing pages (Home, About, Services, Contact)
- [x] User authentication (Login/Register)
- [x] Customer dashboard (Overview, Schedule, Payments, Settings)
- [x] Admin panel (Dashboard, Customers, Messages, Billing)
- [x] Stripe payment integration
- [x] Supabase database schema
- [x] Email notifications (booking, approval, payment)

## What's Been Implemented (March 2026)

### Design System
- Futuristic "Clinical Luxury" theme
- Glassmorphism navbar and cards
- Color palette: Deep Space Blue (#0F172A), Cyan accent (#06B6D4)
- Responsive mobile-first design

### Public Pages
- **Homepage**: Hero with clean background, services preview, why us section, CTAs
- **About**: Tagline, core values (People Over Profit, Integrity, Community, Stephanie Standard)
- **Services**: 6 service cards in grid layout with pricing
- **Contact**: Form + contact info

### Authentication
- Login/Register forms with Supabase Auth
- Role-based access (customer/admin)
- Dashboard protection

### Customer Dashboard
- Dashboard overview with stats
- Schedule cleaning appointments
- Payment history with Stripe integration
- Account settings

### Admin Panel
- Overview with revenue stats
- Customer management
- Message system (individual + broadcast)
- Billing & revenue tracking

### Email Notifications (NEW)
- **New Booking**: Customer receives confirmation, Admin receives notification
- **Booking Approved**: Customer notified to complete payment
- **Payment Confirmed**: Customer and Admin notified

### Database
- Users, Services, Add-ons, Appointments, Payments, Messages tables
- Row Level Security policies
- Seed data for services and add-ons

## Configuration
- Supabase URL: https://lihyhtcjrmkivmbvgnqp.supabase.co
- Stripe Public Key: pk_test_51T6671Gq9xS4z8Cj...
- Admin Email: stephanie.maidtoclean@gmail.com

## Vercel Environment Variables Required
- SUPABASE_URL
- SUPABASE_ANON_KEY
- SUPABASE_SERVICE_ROLE_KEY
- STRIPE_SECRET_KEY
- STRIPE_WEBHOOK_SECRET
- STRIPE_PUBLIC_KEY
- RESEND_API_KEY
- SENDER_EMAIL (or use default: onboarding@resend.dev)
- ADMIN_EMAIL (stephanie.maidtoclean@gmail.com)
- BASE_URL (https://maid-to-clean.vercel.app)

## Prioritized Backlog

### P0 (Critical)
- [x] Deploy updated files to Vercel
- [ ] Run Supabase schema migration
- [ ] Run seed data
- [ ] Set up Resend API key in Vercel

### P1 (Important)
- [ ] Custom email domain (notifications@maidtoclean.com)
- [ ] SMS reminders via Twilio
- [ ] Real-time booking calendar

### P2 (Nice to Have)
- [ ] Customer reviews/testimonials
- [ ] Referral system
- [ ] Staff assignment
- [ ] Analytics dashboard

## Next Tasks
1. Push code to GitHub to trigger Vercel deployment
2. Add Resend API key to Vercel environment variables
3. Run schema.sql in Supabase SQL Editor
4. Run seed.sql to populate services/add-ons
5. Test full booking + payment + email flow
6. Create admin user in database
</file>

<file path="public/admin/admin-billing.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Billing | Maid To Clean Admin</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/admin.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/dashboard-protect.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <main class="admin-container">
    <div class="admin-header">
      <h1>Billing & Revenue</h1>
      <a href="/admin/admin-dashboard.html" class="btn btn-secondary btn-sm">
        <i class="ph-duotone ph-arrow-left"></i> Back to Dashboard
      </a>
    </div>

    <!-- Revenue Stats -->
    <div class="admin-stats-grid">
      <div class="admin-stat-card">
        <h4>Total Revenue</h4>
        <div class="value" id="totalRevenue">$0</div>
      </div>
      <div class="admin-stat-card">
        <h4>This Month</h4>
        <div class="value" id="monthRevenue">$0</div>
      </div>
      <div class="admin-stat-card">
        <h4>Pending Payments</h4>
        <div class="value" id="pendingPayments">$0</div>
      </div>
      <div class="admin-stat-card">
        <h4>Total Transactions</h4>
        <div class="value" id="totalTransactions">0</div>
      </div>
    </div>

    <!-- Payment History -->
    <div class="admin-card">
      <div class="admin-card-header">
        <h3><i class="ph-duotone ph-receipt"></i> Payment History</h3>
        <select id="paymentFilter" style="padding: 0.5rem 1rem; border: 1px solid var(--border); border-radius: var(--radius-sm);">
          <option value="">All Payments</option>
          <option value="paid">Completed</option>
          <option value="pending">Pending</option>
        </select>
      </div>
      <div class="admin-card-body">
        <div id="paymentList" style="overflow-x: auto;">
          <div class="empty-state">
            <i class="ph-duotone ph-receipt"></i>
            <p>Loading payments...</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Customer Billing Summary -->
    <div class="admin-card mt-6">
      <div class="admin-card-header">
        <h3><i class="ph-duotone ph-users"></i> Customer Billing Summary</h3>
      </div>
      <div class="admin-card-body">
        <div id="customerBilling" style="overflow-x: auto;">
          <div class="empty-state">
            <i class="ph-duotone ph-chart-bar"></i>
            <p>Loading billing data...</p>
          </div>
        </div>
      </div>
    </div>
  </main>

  <!-- Toast Container -->
  <div id="toastContainer" class="toast-container"></div>

  <script>
    let allPayments = [];

    function showToast(message, type = 'info') {
      const container = document.getElementById('toastContainer');
      const toast = document.createElement('div');
      toast.className = `toast ${type}`;
      toast.innerHTML = `
        <i class="ph-duotone ph-${type === 'success' ? 'check-circle' : type === 'error' ? 'x-circle' : 'info'}"></i>
        <span>${message}</span>
      `;
      container.appendChild(toast);
      setTimeout(() => toast.remove(), 4000);
    }

    document.addEventListener('DOMContentLoaded', async () => {
      await loadBillingData();

      document.getElementById('paymentFilter').addEventListener('change', (e) => {
        const status = e.target.value;
        const filtered = status 
          ? allPayments.filter(p => p.status === status)
          : allPayments;
        renderPayments(filtered);
      });
    });

    async function loadBillingData() {
      try {
        // Get all payments
        const { data: payments, error } = await supabase
          .from('payments')
          .select(`
            *,
            users (email, full_name),
            appointments (
              appointment_date,
              services (name)
            )
          `)
          .order('created_at', { ascending: false });

        if (error) throw error;

        allPayments = payments || [];

        // Calculate stats
        const paidPayments = allPayments.filter(p => p.status === 'paid');
        const pendingPayments = allPayments.filter(p => p.status === 'pending');

        const totalRevenue = paidPayments.reduce((sum, p) => sum + (p.amount || 0), 0);
        const pendingAmount = pendingPayments.reduce((sum, p) => sum + (p.amount || 0), 0);

        document.getElementById('totalRevenue').textContent = `$${totalRevenue.toFixed(2)}`;
        document.getElementById('pendingPayments').textContent = `$${pendingAmount.toFixed(2)}`;
        document.getElementById('totalTransactions').textContent = paidPayments.length;

        // This month's revenue
        const thisMonth = new Date();
        const monthStart = new Date(thisMonth.getFullYear(), thisMonth.getMonth(), 1).toISOString();
        const monthPayments = paidPayments.filter(p => p.created_at >= monthStart);
        const monthRevenue = monthPayments.reduce((sum, p) => sum + (p.amount || 0), 0);
        document.getElementById('monthRevenue').textContent = `$${monthRevenue.toFixed(2)}`;

        renderPayments(allPayments);
        await loadCustomerBilling();

      } catch (error) {
        console.error('Error loading billing data:', error);
        showToast('Failed to load billing data', 'error');
      }
    }

    function renderPayments(payments) {
      const container = document.getElementById('paymentList');

      if (!payments || payments.length === 0) {
        container.innerHTML = `
          <div class="empty-state">
            <i class="ph-duotone ph-receipt"></i>
            <p>No payments found</p>
          </div>
        `;
        return;
      }

      container.innerHTML = `
        <table class="admin-table">
          <thead>
            <tr>
              <th>Date</th>
              <th>Customer</th>
              <th>Service</th>
              <th>Amount</th>
              <th>Status</th>
              <th>Stripe ID</th>
            </tr>
          </thead>
          <tbody>
            ${payments.map(payment => `
              <tr>
                <td>${new Date(payment.created_at).toLocaleDateString()}</td>
                <td>
                  <div>
                    <p style="font-weight: 500;">${payment.users?.full_name || 'Unknown'}</p>
                    <small style="color: var(--secondary);">${payment.users?.email || ''}</small>
                  </div>
                </td>
                <td>${payment.appointments?.services?.name || 'Cleaning'}</td>
                <td style="font-weight: 600;">$${payment.amount?.toFixed(2) || '0.00'}</td>
                <td><span class="status-badge ${payment.status}">${payment.status}</span></td>
                <td style="font-size: 0.75rem; color: var(--secondary);">${payment.stripe_session_id ? payment.stripe_session_id.substring(0, 15) + '...' : '-'}</td>
              </tr>
            `).join('')}
          </tbody>
        </table>
      `;
    }

    async function loadCustomerBilling() {
      try {
        // Get customer payment summaries
        const { data: customers } = await supabase
          .from('users')
          .select('id, email, full_name')
          .eq('role', 'customer');

        if (!customers) return;

        // Calculate totals for each customer
        const customerSummaries = await Promise.all(customers.map(async (customer) => {
          const { data: payments } = await supabase
            .from('payments')
            .select('amount, status')
            .eq('user_id', customer.id);

          const paid = payments?.filter(p => p.status === 'paid').reduce((sum, p) => sum + (p.amount || 0), 0) || 0;
          const pending = payments?.filter(p => p.status === 'pending').reduce((sum, p) => sum + (p.amount || 0), 0) || 0;

          return {
            ...customer,
            totalPaid: paid,
            pending: pending
          };
        }));

        // Sort by total paid (descending)
        customerSummaries.sort((a, b) => b.totalPaid - a.totalPaid);

        const container = document.getElementById('customerBilling');

        if (customerSummaries.length === 0) {
          return;
        }

        container.innerHTML = `
          <table class="admin-table">
            <thead>
              <tr>
                <th>Customer</th>
                <th>Total Paid</th>
                <th>Pending</th>
              </tr>
            </thead>
            <tbody>
              ${customerSummaries.map(customer => `
                <tr>
                  <td>
                    <div style="display: flex; align-items: center; gap: 0.75rem;">
                      <div class="customer-avatar">${(customer.full_name || customer.email || 'U').charAt(0).toUpperCase()}</div>
                      <div>
                        <p style="font-weight: 500;">${customer.full_name || 'No name'}</p>
                        <small style="color: var(--secondary);">${customer.email}</small>
                      </div>
                    </div>
                  </td>
                  <td style="font-weight: 600; color: var(--success);">$${customer.totalPaid.toFixed(2)}</td>
                  <td style="color: ${customer.pending > 0 ? '#D97706' : 'var(--secondary)'};">$${customer.pending.toFixed(2)}</td>
                </tr>
              `).join('')}
            </tbody>
          </table>
        `;

      } catch (error) {
        console.error('Error loading customer billing:', error);
      }
    }
  </script>

</body>
</html>
</file>

<file path="public/admin/admin-customers.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Customers | Maid To Clean Admin</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/admin.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/dashboard-protect.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <main class="admin-container">
    <div class="admin-header">
      <h1>Customers</h1>
      <a href="/admin/admin-dashboard.html" class="btn btn-secondary btn-sm">
        <i class="ph-duotone ph-arrow-left"></i> Back to Dashboard
      </a>
    </div>

    <!-- Customer List -->
    <div class="admin-card">
      <div class="admin-card-header">
        <h3><i class="ph-duotone ph-users"></i> All Customers</h3>
        <div style="display: flex; gap: 0.5rem;">
          <input type="text" id="searchCustomers" placeholder="Search customers..." style="padding: 0.5rem 1rem; border: 1px solid var(--border); border-radius: var(--radius-sm);" data-testid="search-customers-input">
        </div>
      </div>
      <div class="admin-card-body">
        <div id="customerList" style="overflow-x: auto;">
          <div class="empty-state">
            <i class="ph-duotone ph-users"></i>
            <p>Loading customers...</p>
          </div>
        </div>
      </div>
    </div>

    <!-- All Appointments -->
    <div class="admin-card mt-6">
      <div class="admin-card-header">
        <h3><i class="ph-duotone ph-calendar-check"></i> All Appointments</h3>
        <select id="statusFilter" style="padding: 0.5rem 1rem; border: 1px solid var(--border); border-radius: var(--radius-sm);">
          <option value="">All Statuses</option>
          <option value="pending">Pending</option>
          <option value="approved">Approved</option>
          <option value="paid">Paid</option>
          <option value="completed">Completed</option>
          <option value="cancelled">Cancelled</option>
        </select>
      </div>
      <div class="admin-card-body">
        <div id="appointmentList" style="overflow-x: auto;">
          <div class="empty-state">
            <i class="ph-duotone ph-calendar-blank"></i>
            <p>Loading appointments...</p>
          </div>
        </div>
      </div>
    </div>
  </main>

  <!-- Toast Container -->
  <div id="toastContainer" class="toast-container"></div>

  <script>
    let allCustomers = [];
    let allAppointments = [];

    function showToast(message, type = 'info') {
      const container = document.getElementById('toastContainer');
      const toast = document.createElement('div');
      toast.className = `toast ${type}`;
      toast.innerHTML = `
        <i class="ph-duotone ph-${type === 'success' ? 'check-circle' : type === 'error' ? 'x-circle' : 'info'}"></i>
        <span>${message}</span>
      `;
      container.appendChild(toast);
      setTimeout(() => toast.remove(), 4000);
    }

    document.addEventListener('DOMContentLoaded', async () => {
      await loadCustomers();
      await loadAppointments();

      // Search functionality
      document.getElementById('searchCustomers').addEventListener('input', (e) => {
        const search = e.target.value.toLowerCase();
        const filtered = allCustomers.filter(c => 
          (c.full_name || '').toLowerCase().includes(search) ||
          (c.email || '').toLowerCase().includes(search)
        );
        renderCustomers(filtered);
      });

      // Status filter
      document.getElementById('statusFilter').addEventListener('change', (e) => {
        const status = e.target.value;
        const filtered = status 
          ? allAppointments.filter(a => a.status === status)
          : allAppointments;
        renderAppointments(filtered);
      });
    });

    async function loadCustomers() {
      try {
        const { data: customers, error } = await supabase
          .from('users')
          .select('*')
          .eq('role', 'customer')
          .order('created_at', { ascending: false });

        if (error) throw error;

        allCustomers = customers || [];
        renderCustomers(allCustomers);
      } catch (error) {
        console.error('Error loading customers:', error);
        showToast('Failed to load customers', 'error');
      }
    }

    function renderCustomers(customers) {
      const container = document.getElementById('customerList');
      
      if (!customers || customers.length === 0) {
        container.innerHTML = `
          <div class="empty-state">
            <i class="ph-duotone ph-users"></i>
            <p>No customers found</p>
          </div>
        `;
        return;
      }

      container.innerHTML = `
        <table class="admin-table">
          <thead>
            <tr>
              <th>Customer</th>
              <th>Phone</th>
              <th>Joined</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            ${customers.map(customer => `
              <tr>
                <td>
                  <div style="display: flex; align-items: center; gap: 0.75rem;">
                    <div class="customer-avatar">${(customer.full_name || customer.email || 'U').charAt(0).toUpperCase()}</div>
                    <div>
                      <p style="font-weight: 500;">${customer.full_name || 'No name'}</p>
                      <small style="color: var(--secondary);">${customer.email}</small>
                    </div>
                  </div>
                </td>
                <td>${customer.phone || '-'}</td>
                <td>${new Date(customer.created_at).toLocaleDateString()}</td>
                <td>
                  <button class="action-btn view" onclick="sendMessage('${customer.id}')" data-testid="message-btn-${customer.id}">
                    <i class="ph-duotone ph-envelope"></i>
                  </button>
                </td>
              </tr>
            `).join('')}
          </tbody>
        </table>
      `;
    }

    async function loadAppointments() {
      try {
        const { data: appointments, error } = await supabase
          .from('appointments')
          .select(`
            *,
            users (email, full_name),
            services (name, base_price)
          `)
          .order('created_at', { ascending: false });

        if (error) throw error;

        allAppointments = appointments || [];
        renderAppointments(allAppointments);
      } catch (error) {
        console.error('Error loading appointments:', error);
        showToast('Failed to load appointments', 'error');
      }
    }

    function renderAppointments(appointments) {
      const container = document.getElementById('appointmentList');
      
      if (!appointments || appointments.length === 0) {
        container.innerHTML = `
          <div class="empty-state">
            <i class="ph-duotone ph-calendar-blank"></i>
            <p>No appointments found</p>
          </div>
        `;
        return;
      }

      container.innerHTML = `
        <table class="admin-table">
          <thead>
            <tr>
              <th>Customer</th>
              <th>Service</th>
              <th>Date</th>
              <th>Price</th>
              <th>Status</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            ${appointments.map(apt => `
              <tr>
                <td>${apt.users?.full_name || apt.users?.email || 'Unknown'}</td>
                <td>${apt.services?.name || 'Cleaning'}</td>
                <td>${new Date(apt.appointment_date).toLocaleDateString()} ${apt.appointment_time}</td>
                <td>$${apt.quoted_price || apt.services?.base_price || '-'}</td>
                <td><span class="status-badge ${apt.status}">${apt.status}</span></td>
                <td>
                  ${apt.status === 'pending' ? `
                    <button class="action-btn view" onclick="updateStatus('${apt.id}', 'approved')" data-testid="approve-apt-${apt.id}">
                      <i class="ph-duotone ph-check"></i> Approve
                    </button>
                  ` : ''}
                  ${apt.status === 'paid' ? `
                    <button class="action-btn view" onclick="updateStatus('${apt.id}', 'completed')" data-testid="complete-apt-${apt.id}">
                      <i class="ph-duotone ph-check-circle"></i> Complete
                    </button>
                  ` : ''}
                  ${apt.status !== 'cancelled' && apt.status !== 'completed' ? `
                    <button class="action-btn delete" onclick="updateStatus('${apt.id}', 'cancelled')" data-testid="cancel-apt-${apt.id}">
                      <i class="ph-duotone ph-x"></i>
                    </button>
                  ` : ''}
                </td>
              </tr>
            `).join('')}
          </tbody>
        </table>
      `;
    }

    async function updateStatus(appointmentId, newStatus) {
      try {
        const updates = { status: newStatus };
        
        // If approving, set a quoted price
        if (newStatus === 'approved') {
          const apt = allAppointments.find(a => a.id === appointmentId);
          updates.quoted_price = apt?.services?.base_price || 100;
        }

        const { error } = await supabase
          .from('appointments')
          .update(updates)
          .eq('id', appointmentId);

        if (error) throw error;

        showToast(`Appointment ${newStatus}!`, 'success');
        await loadAppointments();
      } catch (error) {
        console.error('Error updating status:', error);
        showToast('Failed to update status', 'error');
      }
    }

    function sendMessage(customerId) {
      const message = prompt('Enter message to send to customer:');
      if (!message) return;

      sendMessageToCustomer(customerId, message);
    }

    async function sendMessageToCustomer(customerId, content) {
      try {
        const admin = await getCurrentUser();
        
        const { error } = await supabase.from('messages').insert([{
          sender_id: admin.id,
          receiver_id: customerId,
          content: content
        }]);

        if (error) throw error;

        showToast('Message sent!', 'success');
      } catch (error) {
        console.error('Error sending message:', error);
        showToast('Failed to send message', 'error');
      }
    }
  </script>

</body>
</html>
</file>

<file path="public/admin/admin-messages.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Messages | Maid To Clean Admin</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/admin.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/dashboard-protect.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <main class="admin-container">
    <div class="admin-header">
      <h1>Messages</h1>
      <a href="/admin/admin-dashboard.html" class="btn btn-secondary btn-sm">
        <i class="ph-duotone ph-arrow-left"></i> Back to Dashboard
      </a>
    </div>

    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 2rem;">
      <!-- Send Message -->
      <div class="admin-card">
        <div class="admin-card-header">
          <h3><i class="ph-duotone ph-paper-plane-tilt"></i> Send Message</h3>
        </div>
        <div class="admin-card-body">
          <form id="sendMessageForm">
            <div class="form-group">
              <label for="recipientSelect">Select Recipient</label>
              <select id="recipientSelect" required data-testid="recipient-select">
                <option value="">Choose a customer...</option>
                <option value="all">All Customers (Broadcast)</option>
              </select>
            </div>
            <div class="form-group">
              <label for="messageContent">Message</label>
              <textarea id="messageContent" placeholder="Type your message..." required data-testid="message-content-input"></textarea>
            </div>
            <button type="submit" class="btn btn-primary w-full" data-testid="send-message-btn">
              <i class="ph-duotone ph-paper-plane-tilt"></i> Send Message
            </button>
          </form>
        </div>
      </div>

      <!-- Message History -->
      <div class="admin-card">
        <div class="admin-card-header">
          <h3><i class="ph-duotone ph-clock-counter-clockwise"></i> Sent Messages</h3>
        </div>
        <div class="admin-card-body">
          <div id="messageHistory">
            <div class="empty-state">
              <i class="ph-duotone ph-chats"></i>
              <p>No messages sent yet</p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Contact Form Submissions -->
    <div class="admin-card mt-6">
      <div class="admin-card-header">
        <h3><i class="ph-duotone ph-envelope"></i> Contact Form Submissions</h3>
      </div>
      <div class="admin-card-body">
        <div id="contactSubmissions">
          <div class="empty-state">
            <i class="ph-duotone ph-envelope-simple"></i>
            <p>No contact form submissions yet</p>
          </div>
        </div>
      </div>
    </div>
  </main>

  <!-- Toast Container -->
  <div id="toastContainer" class="toast-container"></div>

  <script>
    function showToast(message, type = 'info') {
      const container = document.getElementById('toastContainer');
      const toast = document.createElement('div');
      toast.className = `toast ${type}`;
      toast.innerHTML = `
        <i class="ph-duotone ph-${type === 'success' ? 'check-circle' : type === 'error' ? 'x-circle' : 'info'}"></i>
        <span>${message}</span>
      `;
      container.appendChild(toast);
      setTimeout(() => toast.remove(), 4000);
    }

    document.addEventListener('DOMContentLoaded', async () => {
      await loadCustomersForSelect();
      await loadMessageHistory();
    });

    async function loadCustomersForSelect() {
      try {
        const { data: customers } = await supabase
          .from('users')
          .select('id, email, full_name')
          .eq('role', 'customer')
          .order('full_name');

        const select = document.getElementById('recipientSelect');
        
        if (customers) {
          customers.forEach(c => {
            const option = document.createElement('option');
            option.value = c.id;
            option.textContent = `${c.full_name || 'No name'} (${c.email})`;
            select.appendChild(option);
          });
        }
      } catch (error) {
        console.error('Error loading customers:', error);
      }
    }

    async function loadMessageHistory() {
      try {
        const admin = await getCurrentUser();
        
        const { data: messages, error } = await supabase
          .from('messages')
          .select(`
            *,
            receiver:receiver_id (email, full_name)
          `)
          .eq('sender_id', admin.id)
          .order('created_at', { ascending: false })
          .limit(20);

        if (error) throw error;

        const container = document.getElementById('messageHistory');

        if (!messages || messages.length === 0) {
          return;
        }

        container.innerHTML = `
          <div style="max-height: 400px; overflow-y: auto;">
            ${messages.map(msg => `
              <div style="padding: 1rem; background: var(--background); border-radius: var(--radius-md); margin-bottom: 0.75rem;">
                <div style="display: flex; justify-content: space-between; margin-bottom: 0.5rem;">
                  <span style="font-weight: 500; font-size: 0.875rem;">
                    To: ${msg.receiver?.full_name || msg.receiver?.email || 'Unknown'}
                  </span>
                  <small style="color: var(--secondary);">${new Date(msg.created_at).toLocaleDateString()}</small>
                </div>
                <p style="font-size: 0.9375rem; color: var(--secondary);">${msg.content}</p>
              </div>
            `).join('')}
          </div>
        `;
      } catch (error) {
        console.error('Error loading messages:', error);
      }
    }

    document.getElementById('sendMessageForm').addEventListener('submit', async (e) => {
      e.preventDefault();

      const recipientId = document.getElementById('recipientSelect').value;
      const content = document.getElementById('messageContent').value.trim();

      if (!content) {
        showToast('Please enter a message', 'error');
        return;
      }

      const submitBtn = e.target.querySelector('button[type="submit"]');
      submitBtn.disabled = true;
      submitBtn.innerHTML = '<span class="loading"></span> Sending...';

      try {
        const admin = await getCurrentUser();

        if (recipientId === 'all') {
          // Broadcast to all customers
          const { data: customers } = await supabase
            .from('users')
            .select('id')
            .eq('role', 'customer');

          if (customers && customers.length > 0) {
            const messages = customers.map(c => ({
              sender_id: admin.id,
              receiver_id: c.id,
              content: content
            }));

            const { error } = await supabase.from('messages').insert(messages);
            if (error) throw error;

            showToast(`Message sent to ${customers.length} customers!`, 'success');
          }
        } else {
          // Send to single customer
          const { error } = await supabase.from('messages').insert([{
            sender_id: admin.id,
            receiver_id: recipientId,
            content: content
          }]);

          if (error) throw error;

          showToast('Message sent!', 'success');
        }

        document.getElementById('messageContent').value = '';
        await loadMessageHistory();

      } catch (error) {
        console.error('Error sending message:', error);
        showToast('Failed to send message', 'error');
      } finally {
        submitBtn.disabled = false;
        submitBtn.innerHTML = '<i class="ph-duotone ph-paper-plane-tilt"></i> Send Message';
      }
    });
  </script>

  <style>
    @media (max-width: 900px) {
      .admin-container > div[style*="grid-template-columns: 1fr 1fr"] {
        grid-template-columns: 1fr !important;
      }
    }
  </style>

</body>
</html>
</file>

<file path="public/assets/css/admin.css">
/* ================================
   ADMIN PANEL STYLES
================================ */

.admin-layout {
  display: flex;
  min-height: 100vh;
  padding-top: 80px;
}

/* Admin Sidebar */
.admin-sidebar {
  width: 260px;
  background: var(--primary);
  padding: 2rem 0;
  position: fixed;
  top: 80px;
  left: 0;
  bottom: 0;
  overflow-y: auto;
}

.admin-sidebar-header {
  padding: 0 1.5rem 1.5rem;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  margin-bottom: 1.5rem;
}

.admin-sidebar-header h3 {
  color: white;
  font-size: 1rem;
  font-weight: 600;
}

.admin-sidebar-header p {
  color: rgba(255, 255, 255, 0.6);
  font-size: 0.875rem;
}

.admin-nav {
  list-style: none;
}

.admin-nav a {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.875rem 1.5rem;
  color: rgba(255, 255, 255, 0.7);
  font-weight: 500;
  transition: all 0.2s ease;
  border-left: 3px solid transparent;
}

.admin-nav a:hover,
.admin-nav a.active {
  background: rgba(255, 255, 255, 0.1);
  color: white;
  border-left-color: var(--accent);
}

.admin-nav a i {
  font-size: 1.25rem;
  width: 24px;
}

/* Admin Main */
.admin-main {
  flex: 1;
  margin-left: 260px;
  padding: 2rem 3rem;
  background: var(--background);
}

.admin-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 2rem;
}

.admin-header h1 {
  font-size: 1.75rem;
}

/* Admin Container (for simpler pages) */
.admin-container {
  max-width: 1400px;
  margin: 0 auto;
  padding: 2rem;
  padding-top: calc(80px + 2rem);
  min-height: 100vh;
  background: var(--background);
}

.admin-container h1 {
  font-size: 2rem;
  margin-bottom: 2rem;
}

/* Admin Stats */
.admin-stats-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1.5rem;
  margin-bottom: 2rem;
}

.admin-stat-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: 1.5rem;
  position: relative;
  overflow: hidden;
}

.admin-stat-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 4px;
  height: 100%;
  background: var(--gradient-primary);
}

.admin-stat-card h4 {
  font-size: 0.8125rem;
  font-weight: 500;
  color: var(--secondary);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: 0.5rem;
}

.admin-stat-card .value {
  font-family: var(--font-heading);
  font-size: 2.25rem;
  font-weight: 700;
  color: var(--primary);
}

.admin-stat-card .change {
  font-size: 0.875rem;
  margin-top: 0.5rem;
}

.admin-stat-card .change.positive {
  color: var(--success);
}

.admin-stat-card .change.negative {
  color: var(--error);
}

/* Admin Cards */
.admin-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  margin-bottom: 1.5rem;
}

.admin-card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.25rem 1.5rem;
  border-bottom: 1px solid var(--border);
}

.admin-card-header h3 {
  font-size: 1.125rem;
}

.admin-card-body {
  padding: 1.5rem;
}

/* Admin Tables */
.admin-table {
  width: 100%;
  border-collapse: collapse;
}

.admin-table th,
.admin-table td {
  text-align: left;
  padding: 1rem 1.5rem;
  border-bottom: 1px solid var(--border);
}

.admin-table th {
  font-weight: 600;
  font-size: 0.8125rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--secondary);
  background: var(--background);
}

.admin-table tbody tr:hover {
  background: rgba(6, 182, 212, 0.04);
}

.admin-table tbody tr:last-child td {
  border-bottom: none;
}

/* Action Buttons */
.action-btn {
  padding: 0.5rem 0.75rem;
  border: none;
  border-radius: var(--radius-sm);
  cursor: pointer;
  font-size: 0.875rem;
  font-weight: 500;
  transition: all 0.2s ease;
}

.action-btn.view {
  background: rgba(6, 182, 212, 0.1);
  color: var(--accent);
}

.action-btn.edit {
  background: rgba(59, 130, 246, 0.1);
  color: #3B82F6;
}

.action-btn.delete {
  background: rgba(239, 68, 68, 0.1);
  color: var(--error);
}

.action-btn:hover {
  transform: scale(1.05);
}

/* Message Composer */
.message-composer {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: 1.5rem;
}

.message-composer h3 {
  margin-bottom: 1rem;
}

.message-composer textarea {
  min-height: 150px;
  margin-bottom: 1rem;
}

/* Customer List */
.customer-avatar {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--accent) 0%, #3B82F6 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-weight: 600;
  font-size: 1rem;
}

/* Mobile */
@media (max-width: 1200px) {
  .admin-stats-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 900px) {
  .admin-layout {
    flex-direction: column;
    padding-top: 70px;
  }
  
  .admin-sidebar {
    position: static;
    width: 100%;
    padding: 1rem 0;
    top: auto;
    bottom: auto;
  }
  
  .admin-sidebar-header {
    display: none;
  }
  
  .admin-nav {
    display: flex;
    overflow-x: auto;
    padding: 0 1rem;
  }
  
  .admin-nav a {
    padding: 0.75rem 1rem;
    white-space: nowrap;
    border-left: none;
    border-bottom: 3px solid transparent;
  }
  
  .admin-nav a:hover,
  .admin-nav a.active {
    border-left: none;
    border-bottom-color: var(--accent);
  }
  
  .admin-main {
    margin-left: 0;
    padding: 1.5rem;
  }
  
  .admin-container {
    padding: 1.5rem;
    padding-top: calc(70px + 1.5rem);
  }
  
  .admin-stats-grid {
    grid-template-columns: 1fr;
  }
  
  .admin-table {
    display: block;
    overflow-x: auto;
  }
}
</file>

<file path="public/assets/css/dashboard.css">
/* ================================
   DASHBOARD STYLES
================================ */

.dashboard-layout {
  display: flex;
  min-height: 100vh;
  padding-top: 80px;
}

/* Sidebar */
.dashboard-sidebar {
  width: 260px;
  background: var(--surface);
  border-right: 1px solid var(--border);
  padding: 2rem 0;
  position: fixed;
  top: 80px;
  left: 0;
  bottom: 0;
  overflow-y: auto;
}

.sidebar-nav {
  list-style: none;
}

.sidebar-nav li {
  margin-bottom: 0.25rem;
}

.sidebar-nav a {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.875rem 1.5rem;
  color: var(--secondary);
  font-weight: 500;
  transition: all 0.2s ease;
  border-left: 3px solid transparent;
}

.sidebar-nav a:hover,
.sidebar-nav a.active {
  background: rgba(6, 182, 212, 0.08);
  color: var(--primary);
  border-left-color: var(--accent);
}

.sidebar-nav a i {
  font-size: 1.25rem;
  width: 24px;
}

/* Main Content */
.dashboard-main {
  flex: 1;
  margin-left: 260px;
  padding: 2rem 3rem;
  background: var(--background);
}

.dashboard-header {
  margin-bottom: 2rem;
}

.dashboard-header h1 {
  font-size: 1.75rem;
  margin-bottom: 0.5rem;
}

.dashboard-header p {
  color: var(--secondary);
}

/* Dashboard Container (for pages without sidebar) */
.dashboard-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 2rem;
  padding-top: calc(80px + 2rem);
  min-height: 100vh;
  background: var(--background);
}

.dashboard-container h1 {
  font-size: 2rem;
  margin-bottom: 2rem;
}

/* Stats Grid */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  gap: 1.5rem;
  margin-bottom: 2rem;
}

.stat-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: 1.5rem;
  transition: all 0.2s ease;
}

.stat-card:hover {
  border-color: var(--accent);
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.06);
}

.stat-card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 1rem;
}

.stat-card-header h4 {
  font-size: 0.875rem;
  font-weight: 500;
  color: var(--secondary);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.stat-card-header .icon {
  width: 40px;
  height: 40px;
  background: linear-gradient(135deg, rgba(6, 182, 212, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
}

.stat-card .value {
  font-family: var(--font-heading);
  font-size: 2rem;
  font-weight: 700;
  color: var(--primary);
  line-height: 1;
}

.stat-card .label {
  font-size: 0.875rem;
  color: var(--secondary);
  margin-top: 0.25rem;
}

/* Dashboard Grid */
.dashboard-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 1.5rem;
}

.dashboard-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: 1.5rem;
}

.dashboard-card h3 {
  font-size: 1rem;
  margin-bottom: 1rem;
  padding-bottom: 0.75rem;
  border-bottom: 1px solid var(--border);
}

/* Tables */
.dashboard-table {
  width: 100%;
  border-collapse: collapse;
}

.dashboard-table th,
.dashboard-table td {
  text-align: left;
  padding: 1rem;
  border-bottom: 1px solid var(--border);
}

.dashboard-table th {
  font-weight: 600;
  font-size: 0.875rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--secondary);
  background: var(--background);
}

.dashboard-table tr:hover td {
  background: rgba(6, 182, 212, 0.04);
}

/* Status Badges */
.status-badge {
  display: inline-block;
  padding: 0.25rem 0.75rem;
  border-radius: var(--radius-full);
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.status-badge.pending {
  background: rgba(251, 191, 36, 0.15);
  color: #D97706;
}

.status-badge.approved {
  background: rgba(6, 182, 212, 0.15);
  color: var(--accent);
}

.status-badge.completed,
.status-badge.paid {
  background: rgba(16, 185, 129, 0.15);
  color: var(--success);
}

.status-badge.cancelled {
  background: rgba(239, 68, 68, 0.15);
  color: var(--error);
}

/* Schedule Form */
.schedule-form {
  max-width: 600px;
  background: var(--surface);
  padding: 2rem;
  border-radius: var(--radius-lg);
  border: 1px solid var(--border);
}

.schedule-form label {
  display: block;
  margin-bottom: 0.5rem;
  font-weight: 500;
}

.schedule-form input,
.schedule-form select,
.schedule-form textarea {
  margin-bottom: 1.5rem;
}

.schedule-form select[multiple] {
  min-height: 100px;
}

/* Custom Options */
#customOptions {
  background: var(--background);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  padding: 1.5rem;
  margin-bottom: 1.5rem;
}

#customOptions h3 {
  margin-bottom: 1rem;
  font-size: 1.125rem;
}

#customOptions fieldset {
  border: none;
  margin-bottom: 1rem;
}

#customOptions legend {
  font-weight: 600;
  margin-bottom: 0.75rem;
  color: var(--primary);
}

#customOptions label {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-bottom: 0.5rem;
  font-weight: 400;
  cursor: pointer;
}

#customOptions input[type="checkbox"] {
  width: auto;
  margin-bottom: 0;
  accent-color: var(--accent);
}

/* Empty State */
.empty-state {
  text-align: center;
  padding: 3rem;
  color: var(--secondary);
}

.empty-state i {
  font-size: 3rem;
  margin-bottom: 1rem;
  opacity: 0.5;
}

/* Mobile */
@media (max-width: 900px) {
  .dashboard-layout {
    flex-direction: column;
    padding-top: 70px;
  }
  
  .dashboard-sidebar {
    position: static;
    width: 100%;
    border-right: none;
    border-bottom: 1px solid var(--border);
    padding: 1rem 0;
  }
  
  .sidebar-nav {
    display: flex;
    overflow-x: auto;
    padding: 0 1rem;
  }
  
  .sidebar-nav a {
    padding: 0.75rem 1rem;
    white-space: nowrap;
    border-left: none;
    border-bottom: 3px solid transparent;
  }
  
  .sidebar-nav a:hover,
  .sidebar-nav a.active {
    border-left: none;
    border-bottom-color: var(--accent);
  }
  
  .dashboard-main {
    margin-left: 0;
    padding: 1.5rem;
  }
  
  .dashboard-container {
    padding: 1.5rem;
    padding-top: calc(70px + 1.5rem);
  }
}
</file>

<file path="public/assets/css/main.css">
/* ================================
   MAID TO CLEAN - MAIN CSS
   Clinical Luxury & Futuristic Clean
================================ */

@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Syne:wght@400;600;700;800&display=swap');

/* ================================
   CSS VARIABLES
================================ */
:root {
  /* Colors */
  --background: #F8FAFC;
  --surface: #FFFFFF;
  --primary: #0F172A;
  --secondary: #334155;
  --accent: #06B6D4;
  --accent-glow: 0 0 20px rgba(6, 182, 212, 0.5);
  --border: #E2E8F0;
  --success: #10B981;
  --error: #EF4444;
  
  /* Gradients */
  --hero-overlay: linear-gradient(135deg, rgba(15, 23, 42, 0.85) 0%, rgba(15, 23, 42, 0.4) 100%);
  --gradient-primary: linear-gradient(90deg, #06B6D4 0%, #3B82F6 100%);
  --glass-surface: rgba(255, 255, 255, 0.7);
  
  /* Typography */
  --font-heading: 'Syne', sans-serif;
  --font-body: 'Inter', sans-serif;
  
  /* Spacing */
  --section-spacing: 6rem;
  --container-max: 1280px;
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-full: 9999px;
  
  /* Transitions */
  --transition-fast: 0.2s ease;
  --transition-normal: 0.3s ease;
}

/* ================================
   RESET & BASE
================================ */
*, *::before, *::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: var(--font-body);
  font-weight: 400;
  color: var(--primary);
  background: var(--background);
  line-height: 1.6;
  overflow-x: hidden;
}

img {
  max-width: 100%;
  height: auto;
  display: block;
}

a {
  text-decoration: none;
  color: inherit;
  transition: color var(--transition-fast);
}

/* ================================
   TYPOGRAPHY
================================ */
h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-heading);
  font-weight: 700;
  letter-spacing: -0.02em;
  line-height: 1.2;
  color: var(--primary);
}

h1 { font-size: clamp(2.5rem, 5vw, 4rem); font-weight: 800; }
h2 { font-size: clamp(2rem, 4vw, 3rem); font-weight: 700; }
h3 { font-size: clamp(1.25rem, 2vw, 1.5rem); font-weight: 600; }
h4 { font-size: 1.125rem; font-weight: 600; }

p {
  color: var(--secondary);
  font-size: 1rem;
  line-height: 1.7;
}

.text-accent { color: var(--accent); }
.text-center { text-align: center; }

/* ================================
   LAYOUT
================================ */
.container {
  max-width: var(--container-max);
  margin: 0 auto;
  padding: 0 2rem;
}

section {
  padding: var(--section-spacing) 2rem;
}

/* ================================
   BUTTONS
================================ */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  padding: 1rem 2rem;
  font-family: var(--font-heading);
  font-size: 1rem;
  font-weight: 600;
  border: none;
  border-radius: var(--radius-full);
  cursor: pointer;
  transition: transform var(--transition-fast), box-shadow var(--transition-fast), background var(--transition-fast);
}

.btn-primary {
  background: var(--gradient-primary);
  color: white;
  text-decoration: none;
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: var(--accent-glow);
}

.btn-secondary {
  background: transparent;
  border: 1px solid var(--border);
  color: var(--primary);
}

.btn-secondary:hover {
  background: var(--surface);
  border-color: var(--accent);
}

.btn-outline {
  background: transparent;
  border: 2px solid white;
  color: white;
}

.btn-outline:hover {
  background: rgba(255, 255, 255, 0.1);
  transform: translateY(-2px);
}

.btn-lg {
  padding: 1.25rem 2.5rem;
  font-size: 1.125rem;
}

.btn-sm {
  padding: 0.75rem 1.5rem;
  font-size: 0.875rem;
}

/* ================================
   HERO SECTION
================================ */
.hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.hero-bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  z-index: 1;
}

.hero::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: var(--hero-overlay);
  z-index: 2;
}

.hero-content {
  position: relative;
  z-index: 3;
  text-align: center;
  max-width: 800px;
  padding: 2rem;
}

.hero-content h1 {
  color: white;
  margin-bottom: 1.5rem;
  text-shadow: 0 2px 20px rgba(0, 0, 0, 0.3);
}

.hero-content p {
  color: rgba(255, 255, 255, 0.9);
  font-size: 1.25rem;
  margin-bottom: 2rem;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}

.hero-buttons {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
}

/* ================================
   GLASS CARDS
================================ */
.glass-card {
  background: var(--glass-surface);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.5);
  border-radius: var(--radius-lg);
  padding: 2rem;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  transition: transform var(--transition-normal), box-shadow var(--transition-normal);
}

.glass-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
}

/* ================================
   FEATURE CARDS
================================ */
.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: 2rem;
  transition: border-color var(--transition-normal), transform var(--transition-normal), box-shadow var(--transition-normal);
}

.card:hover {
  border-color: var(--accent);
  transform: translateY(-4px);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
}

.card-icon {
  width: 56px;
  height: 56px;
  background: linear-gradient(135deg, rgba(6, 182, 212, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 1.5rem;
  color: var(--accent);
  font-size: 1.5rem;
}

.card h3 {
  margin-bottom: 0.75rem;
}

.card p {
  font-size: 0.9375rem;
}

/* ================================
   GRID LAYOUTS
================================ */
.grid {
  display: grid;
  gap: 2rem;
}

.grid-2 {
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
}

.grid-3 {
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
}

.grid-4 {
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
}

/* ================================
   SERVICES SECTION
================================ */
.services-preview {
  background: var(--background);
}

.services-preview h2 {
  text-align: center;
  margin-bottom: 3rem;
}

.service-card {
  position: relative;
  background: var(--surface);
  border-radius: var(--radius-lg);
  overflow: hidden;
  transition: transform var(--transition-normal), box-shadow var(--transition-normal);
}

.service-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 25px 50px rgba(0, 0, 0, 0.12);
}

.service-card-image {
  width: 100%;
  height: 200px;
  object-fit: cover;
}

.service-card-content {
  padding: 1.5rem;
}

.service-card h3 {
  margin-bottom: 0.5rem;
}

.service-card p {
  font-size: 0.9375rem;
  margin-bottom: 1rem;
}

.service-card .price {
  font-family: var(--font-heading);
  font-weight: 700;
  color: var(--accent);
  font-size: 1.25rem;
}

/* ================================
   WHY US SECTION
================================ */
.why-us {
  background: var(--primary);
  color: white;
}

.why-us h2 {
  color: white;
  text-align: center;
  margin-bottom: 3rem;
}

.why-us-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 2rem;
}

.why-us-item {
  padding: 2rem;
  background: rgba(255, 255, 255, 0.05);
  border-radius: var(--radius-lg);
  border: 1px solid rgba(255, 255, 255, 0.1);
  transition: background var(--transition-normal);
}

.why-us-item:hover {
  background: rgba(255, 255, 255, 0.08);
}

.why-us-item .icon {
  font-size: 2rem;
  color: var(--accent);
  margin-bottom: 1rem;
}

.why-us-item h4 {
  color: white;
  margin-bottom: 0.75rem;
}

.why-us-item p {
  color: rgba(255, 255, 255, 0.7);
  font-size: 0.9375rem;
}

/* ================================
   CTA SECTION
================================ */
.cta-section {
  background: linear-gradient(135deg, var(--primary) 0%, #1e293b 100%);
  text-align: center;
  padding: var(--section-spacing) 2rem;
}

.cta-section h2 {
  color: white;
  margin-bottom: 1rem;
}

.cta-section p {
  color: rgba(255, 255, 255, 0.8);
  font-size: 1.125rem;
  margin-bottom: 2rem;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}

/* ================================
   CONTACT CTA
================================ */
.contact-cta {
  background: var(--background);
  text-align: center;
}

.contact-cta h2 {
  margin-bottom: 1rem;
}

.contact-cta p {
  margin-bottom: 0.5rem;
}

.contact-cta a {
  color: var(--accent);
  font-weight: 500;
}

.contact-cta a:hover {
  text-decoration: underline;
}

/* ================================
   FORMS
================================ */
.form-group {
  margin-bottom: 1.5rem;
}

label {
  display: block;
  margin-bottom: 0.5rem;
  font-weight: 500;
  color: var(--primary);
}

input, select, textarea {
  width: 100%;
  padding: 0.875rem 1rem;
  font-family: var(--font-body);
  font-size: 1rem;
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  background: var(--surface);
  color: var(--primary);
  transition: border-color var(--transition-fast), outline var(--transition-fast);
}

input:focus, select:focus, textarea:focus {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
  border-color: transparent;
}

textarea {
  min-height: 120px;
  resize: vertical;
}

/* ================================
   AUTH PAGES
================================ */
.auth-container {
  min-height: calc(100vh - 80px);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 3rem 2rem;
  background: var(--background);
}

.auth-container h1 {
  margin-bottom: 2rem;
  text-align: center;
}

.auth-container form {
  width: 100%;
  max-width: 400px;
  background: var(--surface);
  padding: 2.5rem;
  border-radius: var(--radius-lg);
  border: 1px solid var(--border);
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
}

.auth-container input {
  margin-bottom: 1rem;
}

.auth-container .btn-primary {
  width: 100%;
  margin-top: 0.5rem;
}

.auth-container .auth-link {
  text-align: center;
  margin-top: 1.5rem;
  color: var(--secondary);
}

.auth-container .auth-link a {
  color: var(--accent);
  font-weight: 500;
}

/* ================================
   FOOTER
================================ */
.footer {
  background: var(--primary);
  color: white;
  padding: 4rem 2rem 2rem;
}

.footer-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 2rem;
  max-width: var(--container-max);
  margin: 0 auto;
}

.footer-col h4 {
  color: white;
  margin-bottom: 1rem;
  font-size: 1rem;
}

.footer-col p, .footer-col a {
  color: rgba(255, 255, 255, 0.7);
  font-size: 0.9375rem;
  line-height: 2;
}

.footer-col a:hover {
  color: var(--accent);
}

.footer-bottom {
  max-width: var(--container-max);
  margin: 3rem auto 0;
  padding-top: 2rem;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  text-align: center;
  color: rgba(255, 255, 255, 0.5);
  font-size: 0.875rem;
}

/* ================================
   ABOUT PAGE
================================ */
.about-hero {
  padding: calc(var(--section-spacing) + 80px) 2rem var(--section-spacing);
  background: linear-gradient(135deg, var(--primary) 0%, #1e293b 100%);
  text-align: center;
}

.about-hero h1 {
  color: white;
  margin-bottom: 1.5rem;
}

.about-hero .tagline {
  color: var(--accent);
  font-family: var(--font-heading);
  font-size: 1.5rem;
  font-weight: 600;
  max-width: 700px;
  margin: 0 auto;
}

.core-values {
  background: var(--background);
}

.core-values h2 {
  text-align: center;
  margin-bottom: 1rem;
}

.core-values .intro {
  text-align: center;
  max-width: 600px;
  margin: 0 auto 3rem;
}

.value-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: 2rem;
  text-align: center;
  transition: all var(--transition-normal);
}

.value-card:hover {
  border-color: var(--accent);
  transform: translateY(-4px);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
}

.value-card .icon {
  width: 64px;
  height: 64px;
  background: linear-gradient(135deg, rgba(6, 182, 212, 0.15) 0%, rgba(59, 130, 246, 0.15) 100%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 1.5rem;
  font-size: 1.75rem;
  color: var(--accent);
}

.value-card h3 {
  margin-bottom: 0.75rem;
}

/* ================================
   SERVICES PAGE
================================ */
.services-hero {
  padding: calc(var(--section-spacing) + 80px) 2rem var(--section-spacing);
  background: linear-gradient(135deg, var(--primary) 0%, #1e293b 100%);
  text-align: center;
}

.services-hero h1 {
  color: white;
  margin-bottom: 1rem;
}

.services-hero p {
  color: rgba(255, 255, 255, 0.8);
  font-size: 1.125rem;
  max-width: 600px;
  margin: 0 auto;
}

.services-grid-section {
  background: var(--background);
}

.services-grid-section h2 {
  text-align: center;
  margin-bottom: 3rem;
}

.service-detail-card {
  background: var(--surface);
  border-radius: var(--radius-lg);
  overflow: hidden;
  border: 1px solid var(--border);
  transition: all var(--transition-normal);
}

.service-detail-card:hover {
  border-color: var(--accent);
  transform: translateY(-8px);
  box-shadow: 0 25px 50px rgba(0, 0, 0, 0.1);
}

.service-detail-card img {
  width: 100%;
  height: 220px;
  object-fit: cover;
}

.service-detail-card-body {
  padding: 1.75rem;
}

.service-detail-card h3 {
  margin-bottom: 0.75rem;
}

.service-detail-card p {
  font-size: 0.9375rem;
  margin-bottom: 1.25rem;
}

.service-detail-card .price-tag {
  display: inline-block;
  background: linear-gradient(135deg, rgba(6, 182, 212, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
  color: var(--accent);
  padding: 0.5rem 1rem;
  border-radius: var(--radius-full);
  font-family: var(--font-heading);
  font-weight: 700;
  font-size: 1.125rem;
}

/* ================================
   CONTACT PAGE
================================ */
.contact-hero {
  padding: calc(var(--section-spacing) + 80px) 2rem var(--section-spacing);
  background: linear-gradient(135deg, var(--primary) 0%, #1e293b 100%);
  text-align: center;
}

.contact-hero h1 {
  color: white;
  margin-bottom: 1rem;
}

.contact-hero p {
  color: rgba(255, 255, 255, 0.8);
  font-size: 1.125rem;
}

.contact-section {
  background: var(--background);
}

.contact-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  max-width: 1000px;
  margin: 0 auto;
}

.contact-form-container {
  background: var(--surface);
  padding: 2.5rem;
  border-radius: var(--radius-lg);
  border: 1px solid var(--border);
}

.contact-form-container h3 {
  margin-bottom: 1.5rem;
}

.contact-info h3 {
  margin-bottom: 2rem;
}

.contact-info-item {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  margin-bottom: 1.5rem;
}

.contact-info-item .icon {
  width: 48px;
  height: 48px;
  background: linear-gradient(135deg, rgba(6, 182, 212, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  font-size: 1.25rem;
  flex-shrink: 0;
}

.contact-info-item h4 {
  margin-bottom: 0.25rem;
}

.contact-info-item p, .contact-info-item a {
  color: var(--secondary);
  font-size: 0.9375rem;
}

.contact-info-item a:hover {
  color: var(--accent);
}

@media (max-width: 768px) {
  .contact-grid {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
}

/* ================================
   TOAST NOTIFICATIONS
================================ */
.toast-container {
  position: fixed;
  bottom: 2rem;
  right: 2rem;
  z-index: 9999;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.toast {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  padding: 1rem 1.5rem;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
  display: flex;
  align-items: center;
  gap: 0.75rem;
  animation: slideIn 0.3s ease;
  max-width: 350px;
}

.toast.success { border-left: 4px solid var(--success); }
.toast.error { border-left: 4px solid var(--error); }
.toast.info { border-left: 4px solid var(--accent); }

@keyframes slideIn {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

/* ================================
   LOADING STATES
================================ */
.loading {
  display: inline-block;
  width: 20px;
  height: 20px;
  border: 2px solid var(--border);
  border-top-color: var(--accent);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

/* ================================
   RESPONSIVE
================================ */
@media (max-width: 768px) {
  :root {
    --section-spacing: 4rem;
  }
  
  .hero {
    min-height: 80vh;
  }
  
  .hero-content h1 {
    font-size: 2rem;
  }
  
  .hero-content p {
    font-size: 1rem;
  }
  
  .hero-buttons {
    flex-direction: column;
    align-items: center;
  }
  
  .btn-lg {
    padding: 1rem 2rem;
    font-size: 1rem;
  }
  
  section {
    padding: var(--section-spacing) 1.5rem;
  }
}

@media (max-width: 480px) {
  h1 { font-size: 2rem; }
  h2 { font-size: 1.75rem; }
  
  .container {
    padding: 0 1rem;
  }
}
</file>

<file path="public/assets/css/navbar.css">
/* ================================
   NAVBAR STYLES - Glassmorphism
================================ */

.navbar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border-bottom: 1px solid rgba(226, 232, 240, 0.5);
  padding: 0 2rem;
  z-index: 900;
  transition: background 0.3s ease, box-shadow 0.3s ease;
}

.navbar.scrolled {
  background: rgba(255, 255, 255, 0.95);
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
}

.nav-container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  max-width: 1280px;
  margin: 0 auto;
  height: 80px;
}

/* Logo */
.logo {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  text-decoration: none;
  color: var(--primary);
  font-family: var(--font-heading);
  font-weight: 700;
  font-size: 1.25rem;
}

.logo-img {
  height: 45px;
  width: auto;
  object-fit: contain;
}

.logo-text {
  letter-spacing: -0.02em;
}

/* Nav links */
.nav-links {
  display: flex;
  list-style: none;
  gap: 0.5rem;
  align-items: center;
}

.nav-links li {
  display: block;
}

.nav-links a {
  display: block;
  padding: 0.625rem 1rem;
  text-decoration: none;
  color: var(--secondary);
  font-family: var(--font-body);
  font-size: 0.9375rem;
  font-weight: 500;
  border-radius: var(--radius-sm);
  transition: color 0.2s ease, background 0.2s ease;
}

.nav-links a:hover {
  color: var(--primary);
  background: rgba(6, 182, 212, 0.08);
}

/* CTA Button in Nav */
.nav-cta {
  margin-left: 0.5rem;
}

.nav-cta a {
  background: var(--gradient-primary);
  color: white !important;
  padding: 0.625rem 1.25rem;
  border-radius: var(--radius-full);
  font-weight: 600;
}

.nav-cta a:hover {
  background: var(--gradient-primary);
  transform: translateY(-1px);
  box-shadow: var(--accent-glow);
}

/* Hamburger */
.hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 8px;
  border-radius: var(--radius-sm);
  transition: background 0.2s ease;
}

.hamburger:hover {
  background: rgba(6, 182, 212, 0.08);
}

.hamburger span {
  display: block;
  width: 24px;
  height: 2px;
  background: var(--primary);
  transition: all 0.3s ease;
  border-radius: 2px;
}

.hamburger.active span:nth-child(1) {
  transform: translateY(7px) rotate(45deg);
}

.hamburger.active span:nth-child(2) {
  opacity: 0;
}

.hamburger.active span:nth-child(3) {
  transform: translateY(-7px) rotate(-45deg);
}

/* Role-based visibility - hidden by default */
.guest-only,
.customer-only,
.admin-only,
.auth-only {
  display: none !important;
}

/* Mobile Styles */
@media (max-width: 900px) {
  .hamburger {
    display: flex;
  }

  .nav-links {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    background: rgba(255, 255, 255, 0.98);
    backdrop-filter: blur(12px);
    flex-direction: column;
    padding: 1.5rem;
    border-bottom: 1px solid var(--border);
    gap: 0.5rem;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  }

  .nav-links.open {
    display: flex;
  }

  .nav-links a {
    padding: 1rem;
    font-size: 1rem;
  }
  
  .nav-cta {
    margin-left: 0;
    margin-top: 0.5rem;
    width: 100%;
  }
  
  .nav-cta a {
    display: block;
    text-align: center;
    padding: 1rem;
  }

  .navbar {
    position: sticky;
    padding: 0 1.5rem;
  }
  
  .nav-container {
    height: 70px;
  }
}

@media (max-width: 480px) {
  .logo-text {
    font-size: 1rem;
  }
  
  .logo-img {
    height: 36px;
  }
}
</file>

<file path="public/assets/css/responsive.css">
/* ================================
   RESPONSIVE UTILITIES
================================ */

/* Hide/Show on breakpoints */
@media (max-width: 768px) {
  .hide-mobile {
    display: none !important;
  }
}

@media (min-width: 769px) {
  .hide-desktop {
    display: none !important;
  }
}

/* Flex utilities */
.flex { display: flex; }
.flex-col { flex-direction: column; }
.items-center { align-items: center; }
.justify-center { justify-content: center; }
.justify-between { justify-content: space-between; }
.gap-1 { gap: 0.25rem; }
.gap-2 { gap: 0.5rem; }
.gap-3 { gap: 0.75rem; }
.gap-4 { gap: 1rem; }
.gap-6 { gap: 1.5rem; }
.gap-8 { gap: 2rem; }

/* Spacing utilities */
.mt-1 { margin-top: 0.25rem; }
.mt-2 { margin-top: 0.5rem; }
.mt-3 { margin-top: 0.75rem; }
.mt-4 { margin-top: 1rem; }
.mt-6 { margin-top: 1.5rem; }
.mt-8 { margin-top: 2rem; }

.mb-1 { margin-bottom: 0.25rem; }
.mb-2 { margin-bottom: 0.5rem; }
.mb-3 { margin-bottom: 0.75rem; }
.mb-4 { margin-bottom: 1rem; }
.mb-6 { margin-bottom: 1.5rem; }
.mb-8 { margin-bottom: 2rem; }

.p-4 { padding: 1rem; }
.p-6 { padding: 1.5rem; }
.p-8 { padding: 2rem; }

.px-4 { padding-left: 1rem; padding-right: 1rem; }
.py-4 { padding-top: 1rem; padding-bottom: 1rem; }

/* Text utilities */
.text-sm { font-size: 0.875rem; }
.text-lg { font-size: 1.125rem; }
.text-xl { font-size: 1.25rem; }
.font-medium { font-weight: 500; }
.font-semibold { font-weight: 600; }
.font-bold { font-weight: 700; }

/* Width utilities */
.w-full { width: 100%; }
.max-w-sm { max-width: 24rem; }
.max-w-md { max-width: 28rem; }
.max-w-lg { max-width: 32rem; }
.max-w-xl { max-width: 36rem; }

/* Visibility */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}
</file>

<file path="public/assets/js/auth.js">
// ================================
// AUTH FUNCTIONS
// ================================

async function registerUser(email, password, metadata = {}) {
  const { data, error } = await supabase.auth.signUp({
    email,
    password,
    options: {
      data: metadata
    }
  });

  if (error) throw error;

  // Insert user into users table with customer role
  if (data.user) {
    await supabase.from("users").insert([{
      id: data.user.id,
      email: email,
      role: "customer",
      ...metadata
    }]);
  }

  return data;
}

async function loginUser(email, password) {
  const { data, error } = await supabase.auth.signInWithPassword({
    email,
    password,
  });

  if (error) throw error;
  return data;
}

async function logoutUser() {
  await supabase.auth.signOut();
  window.location.href = "/index.html";
}

async function getCurrentUser() {
  const { data } = await supabase.auth.getUser();
  return data.user;
}

async function getSession() {
  const { data } = await supabase.auth.getSession();
  return data.session;
}

async function getUserRole() {
  const user = await getCurrentUser();
  if (!user) return null;

  const { data, error } = await supabase
    .from("users")
    .select("role")
    .eq("id", user.id)
    .single();

  if (error) {
    console.error("Error fetching user role:", error);
    return null;
  }

  return data?.role;
}

async function getUserProfile() {
  const user = await getCurrentUser();
  if (!user) return null;

  const { data, error } = await supabase
    .from("users")
    .select("*")
    .eq("id", user.id)
    .single();

  if (error) {
    console.error("Error fetching user profile:", error);
    return null;
  }

  return data;
}

// Listen for auth state changes
supabase.auth.onAuthStateChange((event, session) => {
  if (event === 'SIGNED_OUT') {
    // Clear any local state if needed
    console.log('User signed out');
  }
});
</file>

<file path="public/assets/js/load-navbar.js">
// ================================
// LOAD NAVBAR - Handles dynamic navbar loading and auth state
// ================================

fetch("/components/navbar.html")
  .then((res) => res.text())
  .then((html) => {
    document.getElementById("navbar").innerHTML = html;

    // Handle logout button
    const logoutBtn = document.getElementById("logoutBtn");
    if (logoutBtn) {
      logoutBtn.addEventListener("click", (e) => {
        e.preventDefault();
        logoutUser();
      });
    }

    // Handle hamburger menu
    const hamburger = document.getElementById("hamburger");
    const navLinks = document.getElementById("nav-links");
    
    if (hamburger) {
      hamburger.addEventListener("click", () => {
        hamburger.classList.toggle("active");
        navLinks.classList.toggle("open");
        hamburger.setAttribute("aria-expanded", navLinks.classList.contains("open"));
      });
    }

    // Close menu when clicking a link (mobile)
    navLinks.querySelectorAll("a").forEach(link => {
      link.addEventListener("click", () => {
        hamburger.classList.remove("active");
        navLinks.classList.remove("open");
      });
    });

    // Show/hide nav links based on auth state
    updateNavState();

    // Add scroll effect to navbar
    let lastScroll = 0;
    window.addEventListener("scroll", () => {
      const nav = document.getElementById("mainNav");
      const currentScroll = window.pageYOffset;
      
      if (currentScroll > 50) {
        nav.classList.add("scrolled");
      } else {
        nav.classList.remove("scrolled");
      }
      
      lastScroll = currentScroll;
    });
  })
  .catch((err) => console.error("Failed to load navbar:", err));

// Update navigation based on auth state
async function updateNavState() {
  try {
    const session = await getSession();
    
    if (session && session.user) {
      // User is logged in
      document.querySelectorAll(".guest-only").forEach((el) => {
        el.style.display = "none";
      });
      document.querySelectorAll(".auth-only").forEach((el) => {
        el.style.display = "block";
      });

      // Check role for customer/admin links
      const role = await getUserRole();
      
      document.querySelectorAll(".customer-only").forEach((el) => {
        el.style.display = role === "customer" || role === "admin" ? "block" : "none";
      });
      document.querySelectorAll(".admin-only").forEach((el) => {
        el.style.display = role === "admin" ? "block" : "none";
      });
    } else {
      // User is not logged in
      document.querySelectorAll(".guest-only").forEach((el) => {
        el.style.display = "block";
      });
      document.querySelectorAll(".auth-only").forEach((el) => {
        el.style.display = "none";
      });
      document.querySelectorAll(".customer-only").forEach((el) => {
        el.style.display = "none";
      });
      document.querySelectorAll(".admin-only").forEach((el) => {
        el.style.display = "none";
      });
    }
  } catch (error) {
    console.error("Error updating nav state:", error);
    // Default to guest state on error
    document.querySelectorAll(".guest-only").forEach((el) => {
      el.style.display = "block";
    });
  }
}
</file>

<file path="public/dashboard/dashboard.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard | Maid To Clean</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/dashboard.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/dashboard-protect.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <main class="dashboard-container">
    <div class="dashboard-header">
      <h1>Welcome, <span id="userName">Guest</span></h1>
      <p>Here's an overview of your cleaning services</p>
    </div>

    <!-- Stats Grid -->
    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-card-header">
          <h4>Upcoming</h4>
          <div class="icon"><i class="ph-duotone ph-calendar-check"></i></div>
        </div>
        <div class="value" id="upcomingCount">0</div>
        <div class="label">Scheduled appointments</div>
      </div>
      <div class="stat-card">
        <div class="stat-card-header">
          <h4>Completed</h4>
          <div class="icon"><i class="ph-duotone ph-check-circle"></i></div>
        </div>
        <div class="value" id="completedCount">0</div>
        <div class="label">Cleanings this year</div>
      </div>
      <div class="stat-card">
        <div class="stat-card-header">
          <h4>Balance</h4>
          <div class="icon"><i class="ph-duotone ph-wallet"></i></div>
        </div>
        <div class="value" id="balanceAmount">$0</div>
        <div class="label">Outstanding balance</div>
      </div>
    </div>

    <!-- Dashboard Grid -->
    <div class="dashboard-grid">
      <div class="dashboard-card">
        <h3><i class="ph-duotone ph-clock"></i> Next Appointment</h3>
        <div id="nextAppointment">
          <div class="empty-state">
            <i class="ph-duotone ph-calendar-blank"></i>
            <p>No upcoming appointments</p>
            <a href="/dashboard/schedule.html" class="btn btn-primary btn-sm mt-4" data-testid="book-appointment-btn">
              Book Now
            </a>
          </div>
        </div>
      </div>

      <div class="dashboard-card">
        <h3><i class="ph-duotone ph-envelope"></i> Messages</h3>
        <div id="adminMessage">
          <div class="empty-state">
            <i class="ph-duotone ph-chats"></i>
            <p>No messages yet</p>
          </div>
        </div>
      </div>

      <div class="dashboard-card">
        <h3><i class="ph-duotone ph-clock-countdown"></i> Recent Activity</h3>
        <div id="recentActivity">
          <div class="empty-state">
            <i class="ph-duotone ph-list-dashes"></i>
            <p>No recent activity</p>
          </div>
        </div>
      </div>

      <div class="dashboard-card">
        <h3><i class="ph-duotone ph-lightning"></i> Quick Actions</h3>
        <div style="display: flex; flex-direction: column; gap: 0.75rem;">
          <a href="/dashboard/schedule.html" class="btn btn-primary" data-testid="schedule-btn">
            <i class="ph-duotone ph-calendar-plus"></i> Schedule Cleaning
          </a>
          <a href="/dashboard/payments.html" class="btn btn-secondary" data-testid="payments-btn">
            <i class="ph-duotone ph-credit-card"></i> View Payments
          </a>
          <a href="/dashboard/settings.html" class="btn btn-secondary" data-testid="settings-btn">
            <i class="ph-duotone ph-gear"></i> Account Settings
          </a>
        </div>
      </div>
    </div>
  </main>

  <script>
    document.addEventListener('DOMContentLoaded', async () => {
      // Load user info
      const user = await getCurrentUser();
      if (!user) return;

      const profile = await getUserProfile();
      if (profile) {
        document.getElementById('userName').textContent = profile.full_name || profile.email.split('@')[0];
      }

      // Load dashboard data
      await loadDashboardData(user.id);
    });

    async function loadDashboardData(userId) {
      try {
        // Get appointments
        const { data: appointments } = await supabase
          .from('appointments')
          .select('*')
          .eq('user_id', userId)
          .order('appointment_date', { ascending: true });

        if (appointments) {
          const today = new Date().toISOString().split('T')[0];
          const upcoming = appointments.filter(a => a.appointment_date >= today && a.status !== 'completed');
          const completed = appointments.filter(a => a.status === 'completed' || a.status === 'paid');

          document.getElementById('upcomingCount').textContent = upcoming.length;
          document.getElementById('completedCount').textContent = completed.length;

          // Show next appointment
          if (upcoming.length > 0) {
            const next = upcoming[0];
            document.getElementById('nextAppointment').innerHTML = `
              <div style="padding: 1rem; background: var(--background); border-radius: var(--radius-md);">
                <p style="font-weight: 600; margin-bottom: 0.5rem;">${new Date(next.appointment_date).toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric' })}</p>
                <p style="color: var(--secondary);">at ${next.appointment_time}</p>
                <span class="status-badge ${next.status}">${next.status}</span>
              </div>
            `;
          }
        }

        // Get outstanding payments
        const { data: payments } = await supabase
          .from('payments')
          .select('amount, status')
          .eq('user_id', userId)
          .eq('status', 'pending');

        if (payments) {
          const totalBalance = payments.reduce((sum, p) => sum + (p.amount || 0), 0);
          document.getElementById('balanceAmount').textContent = `$${totalBalance.toFixed(2)}`;
        }

        // Get messages
        const { data: messages } = await supabase
          .from('messages')
          .select('*')
          .eq('receiver_id', userId)
          .order('created_at', { ascending: false })
          .limit(3);

        if (messages && messages.length > 0) {
          document.getElementById('adminMessage').innerHTML = messages.map(m => `
            <div style="padding: 0.75rem; background: var(--background); border-radius: var(--radius-sm); margin-bottom: 0.5rem;">
              <p style="font-size: 0.875rem;">${m.content}</p>
              <small style="color: var(--secondary);">${new Date(m.created_at).toLocaleDateString()}</small>
            </div>
          `).join('');
        }

      } catch (error) {
        console.error('Error loading dashboard data:', error);
      }
    }
  </script>

</body>
</html>
</file>

<file path="public/dashboard/payment-success.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payment Successful | Maid To Clean</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <main style="min-height: calc(100vh - 80px); display: flex; align-items: center; justify-content: center; padding: 2rem; padding-top: calc(80px + 2rem);">
    <div style="text-align: center; max-width: 500px;">
      <div style="width: 80px; height: 80px; background: rgba(16, 185, 129, 0.15); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 2rem;">
        <i class="ph-duotone ph-check-circle" style="font-size: 3rem; color: var(--success);"></i>
      </div>
      
      <h1 style="margin-bottom: 1rem;">Payment Successful!</h1>
      <p style="font-size: 1.125rem; color: var(--secondary); margin-bottom: 2rem;">
        Thank you for your payment. Your cleaning service has been confirmed and you'll receive a confirmation email shortly.
      </p>
      
      <div style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;">
        <a href="/dashboard/dashboard.html" class="btn btn-primary" data-testid="back-to-dashboard-btn">
          <i class="ph-duotone ph-house-line"></i>
          Back to Dashboard
        </a>
        <a href="/dashboard/payments.html" class="btn btn-secondary" data-testid="view-payments-btn">
          <i class="ph-duotone ph-receipt"></i>
          View Payments
        </a>
      </div>
    </div>
  </main>

</body>
</html>
</file>

<file path="public/dashboard/payments.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payments | Maid To Clean</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/dashboard.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://js.stripe.com/v3/"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/dashboard-protect.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <main class="dashboard-container">
    <div class="dashboard-header">
      <h1>Payments</h1>
      <p>View and manage your payment history</p>
    </div>

    <!-- Balance Overview -->
    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-card-header">
          <h4>Outstanding</h4>
          <div class="icon"><i class="ph-duotone ph-warning-circle"></i></div>
        </div>
        <div class="value" id="outstandingBalance">$0.00</div>
        <div class="label">Amount due</div>
      </div>
      <div class="stat-card">
        <div class="stat-card-header">
          <h4>Total Paid</h4>
          <div class="icon"><i class="ph-duotone ph-check-circle"></i></div>
        </div>
        <div class="value" id="totalPaid">$0.00</div>
        <div class="label">All time</div>
      </div>
      <div class="stat-card">
        <div class="stat-card-header">
          <h4>Transactions</h4>
          <div class="icon"><i class="ph-duotone ph-receipt"></i></div>
        </div>
        <div class="value" id="transactionCount">0</div>
        <div class="label">Total payments</div>
      </div>
    </div>

    <!-- Pending Payments -->
    <div class="dashboard-card mb-6">
      <h3><i class="ph-duotone ph-clock"></i> Pending Payments</h3>
      <div id="pendingPayments">
        <div class="empty-state">
          <i class="ph-duotone ph-check-circle"></i>
          <p>No pending payments</p>
        </div>
      </div>
    </div>

    <!-- Payment History -->
    <div class="dashboard-card">
      <h3><i class="ph-duotone ph-clock-counter-clockwise"></i> Payment History</h3>
      <div id="paymentHistory" style="overflow-x: auto;">
        <div class="empty-state">
          <i class="ph-duotone ph-receipt"></i>
          <p>No payment history yet</p>
        </div>
      </div>
    </div>
  </main>

  <!-- Toast Container -->
  <div id="toastContainer" class="toast-container"></div>

  <script>
    function showToast(message, type = 'info') {
      const container = document.getElementById('toastContainer');
      const toast = document.createElement('div');
      toast.className = `toast ${type}`;
      toast.innerHTML = `
        <i class="ph-duotone ph-${type === 'success' ? 'check-circle' : type === 'error' ? 'x-circle' : 'info'}"></i>
        <span>${message}</span>
      `;
      container.appendChild(toast);
      setTimeout(() => toast.remove(), 4000);
    }

    document.addEventListener('DOMContentLoaded', async () => {
      const user = await getCurrentUser();
      if (!user) return;

      await loadPaymentData(user.id);
    });

    async function loadPaymentData(userId) {
      try {
        // Get all payments
        const { data: payments, error } = await supabase
          .from('payments')
          .select(`
            *,
            appointments (
              appointment_date,
              services (name)
            )
          `)
          .eq('user_id', userId)
          .order('created_at', { ascending: false });

        if (error) throw error;

        // Calculate totals
        const paidPayments = payments?.filter(p => p.status === 'paid') || [];
        const pendingPayments = payments?.filter(p => p.status === 'pending') || [];

        const totalPaid = paidPayments.reduce((sum, p) => sum + (p.amount || 0), 0);
        const outstanding = pendingPayments.reduce((sum, p) => sum + (p.amount || 0), 0);

        document.getElementById('totalPaid').textContent = `$${totalPaid.toFixed(2)}`;
        document.getElementById('outstandingBalance').textContent = `$${outstanding.toFixed(2)}`;
        document.getElementById('transactionCount').textContent = paidPayments.length;

        // Show pending payments with pay button
        if (pendingPayments.length > 0) {
          document.getElementById('pendingPayments').innerHTML = pendingPayments.map(payment => `
            <div style="display: flex; justify-content: space-between; align-items: center; padding: 1rem; background: var(--background); border-radius: var(--radius-md); margin-bottom: 0.75rem; border-left: 3px solid #D97706;">
              <div>
                <p style="font-weight: 600;">${payment.appointments?.services?.name || 'Cleaning Service'}</p>
                <p style="font-size: 0.875rem; color: var(--secondary);">
                  ${payment.appointments?.appointment_date ? new Date(payment.appointments.appointment_date).toLocaleDateString() : 'Pending'}
                </p>
              </div>
              <div style="display: flex; align-items: center; gap: 1rem;">
                <span style="font-weight: 700; font-size: 1.125rem;">$${payment.amount?.toFixed(2) || '0.00'}</span>
                <button class="btn btn-primary btn-sm" onclick="initiatePayment('${payment.appointment_id}')" data-testid="pay-btn-${payment.id}">
                  <i class="ph-duotone ph-credit-card"></i> Pay Now
                </button>
              </div>
            </div>
          `).join('');
        }

        // Get approved appointments without payment record
        const { data: approvedAppointments } = await supabase
          .from('appointments')
          .select(`
            *,
            services (name, base_price)
          `)
          .eq('user_id', userId)
          .eq('status', 'approved');

        if (approvedAppointments && approvedAppointments.length > 0) {
          const pendingHtml = approvedAppointments.map(apt => `
            <div style="display: flex; justify-content: space-between; align-items: center; padding: 1rem; background: var(--background); border-radius: var(--radius-md); margin-bottom: 0.75rem; border-left: 3px solid var(--accent);">
              <div>
                <p style="font-weight: 600;">${apt.services?.name || 'Cleaning Service'}</p>
                <p style="font-size: 0.875rem; color: var(--secondary);">
                  ${new Date(apt.appointment_date).toLocaleDateString()} at ${apt.appointment_time}
                </p>
              </div>
              <div style="display: flex; align-items: center; gap: 1rem;">
                <span style="font-weight: 700; font-size: 1.125rem;">$${apt.quoted_price?.toFixed(2) || apt.services?.base_price?.toFixed(2) || '0.00'}</span>
                <button class="btn btn-primary btn-sm" onclick="initiatePayment('${apt.id}')" data-testid="pay-apt-btn-${apt.id}">
                  <i class="ph-duotone ph-credit-card"></i> Pay Now
                </button>
              </div>
            </div>
          `).join('');

          document.getElementById('pendingPayments').innerHTML = pendingHtml;
        }

        // Payment history
        if (paidPayments.length > 0) {
          document.getElementById('paymentHistory').innerHTML = `
            <table class="dashboard-table">
              <thead>
                <tr>
                  <th>Date</th>
                  <th>Service</th>
                  <th>Amount</th>
                  <th>Status</th>
                </tr>
              </thead>
              <tbody>
                ${paidPayments.map(payment => `
                  <tr>
                    <td>${new Date(payment.created_at).toLocaleDateString()}</td>
                    <td>${payment.appointments?.services?.name || 'Cleaning Service'}</td>
                    <td style="font-weight: 600;">$${payment.amount?.toFixed(2) || '0.00'}</td>
                    <td><span class="status-badge paid">Paid</span></td>
                  </tr>
                `).join('')}
              </tbody>
            </table>
          `;
        }

      } catch (error) {
        console.error('Error loading payment data:', error);
        showToast('Failed to load payment data', 'error');
      }
    }

    async function initiatePayment(appointmentId) {
      try {
        showToast('Redirecting to payment...', 'info');

        const response = await fetch('/api/create-checkout-session', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({ appointmentId })
        });

        const data = await response.json();

        if (data.error) {
          throw new Error(data.error);
        }

        if (data.url) {
          window.location.href = data.url;
        }
      } catch (error) {
        console.error('Payment error:', error);
        showToast(error.message || 'Payment initiation failed', 'error');
      }
    }
  </script>

</body>
</html>
</file>

<file path="public/dashboard/settings.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Account Settings | Maid To Clean</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/dashboard.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/dashboard-protect.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <main class="dashboard-container">
    <div class="dashboard-header">
      <h1>Account Settings</h1>
      <p>Manage your profile and preferences</p>
    </div>

    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 2rem;">
      <!-- Profile Settings -->
      <div class="dashboard-card">
        <h3><i class="ph-duotone ph-user"></i> Profile Information</h3>
        <form id="profileForm">
          <div class="form-group">
            <label for="fullName">Full Name</label>
            <input type="text" id="fullName" placeholder="Your name" data-testid="profile-name-input">
          </div>
          <div class="form-group">
            <label for="email">Email Address</label>
            <input type="email" id="email" disabled data-testid="profile-email-input">
          </div>
          <div class="form-group">
            <label for="phone">Phone Number</label>
            <input type="tel" id="phone" placeholder="(334) 123-4567" data-testid="profile-phone-input">
          </div>
          <div class="form-group">
            <label for="address">Address</label>
            <textarea id="address" placeholder="Your service address" data-testid="profile-address-input"></textarea>
          </div>
          <button type="submit" class="btn btn-primary" data-testid="save-profile-btn">
            <i class="ph-duotone ph-floppy-disk"></i>
            Save Changes
          </button>
        </form>
      </div>

      <!-- Password & Security -->
      <div>
        <div class="dashboard-card mb-6">
          <h3><i class="ph-duotone ph-lock"></i> Change Password</h3>
          <form id="passwordForm">
            <div class="form-group">
              <label for="newPassword">New Password</label>
              <input type="password" id="newPassword" placeholder="Enter new password" minlength="6" data-testid="new-password-input">
            </div>
            <div class="form-group">
              <label for="confirmPassword">Confirm Password</label>
              <input type="password" id="confirmPassword" placeholder="Confirm new password" data-testid="confirm-password-input">
            </div>
            <button type="submit" class="btn btn-secondary" data-testid="change-password-btn">
              <i class="ph-duotone ph-key"></i>
              Update Password
            </button>
          </form>
        </div>

        <div class="dashboard-card mb-6">
          <h3><i class="ph-duotone ph-headset"></i> Support</h3>
          <p style="margin-bottom: 1rem; color: var(--secondary);">Need help? Get in touch with our support team.</p>
          <a href="/contact.html" class="btn btn-secondary w-full" data-testid="contact-support-btn">
            <i class="ph-duotone ph-chat-circle-dots"></i>
            Contact Support
          </a>
        </div>

        <div class="dashboard-card" style="border-color: var(--error);">
          <h3 style="color: var(--error);"><i class="ph-duotone ph-warning"></i> Danger Zone</h3>
          <p style="margin-bottom: 1rem; color: var(--secondary);">Once you delete your account, there is no going back. Please be certain.</p>
          <button class="btn" style="background: rgba(239, 68, 68, 0.1); color: var(--error);" onclick="confirmDeleteAccount()" data-testid="delete-account-btn">
            <i class="ph-duotone ph-trash"></i>
            Delete Account
          </button>
        </div>
      </div>
    </div>
  </main>

  <!-- Toast Container -->
  <div id="toastContainer" class="toast-container"></div>

  <script>
    function showToast(message, type = 'info') {
      const container = document.getElementById('toastContainer');
      const toast = document.createElement('div');
      toast.className = `toast ${type}`;
      toast.innerHTML = `
        <i class="ph-duotone ph-${type === 'success' ? 'check-circle' : type === 'error' ? 'x-circle' : 'info'}"></i>
        <span>${message}</span>
      `;
      container.appendChild(toast);
      setTimeout(() => toast.remove(), 4000);
    }

    document.addEventListener('DOMContentLoaded', async () => {
      const user = await getCurrentUser();
      if (!user) return;

      // Load profile data
      const profile = await getUserProfile();
      if (profile) {
        document.getElementById('fullName').value = profile.full_name || '';
        document.getElementById('email').value = profile.email || user.email;
        document.getElementById('phone').value = profile.phone || '';
        document.getElementById('address').value = profile.address || '';
      } else {
        document.getElementById('email').value = user.email;
      }
    });

    // Save profile
    document.getElementById('profileForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const user = await getCurrentUser();
      if (!user) return;

      const submitBtn = e.target.querySelector('button[type="submit"]');
      submitBtn.disabled = true;
      submitBtn.innerHTML = '<span class="loading"></span> Saving...';

      try {
        const { error } = await supabase
          .from('users')
          .update({
            full_name: document.getElementById('fullName').value,
            phone: document.getElementById('phone').value,
            address: document.getElementById('address').value
          })
          .eq('id', user.id);

        if (error) throw error;

        showToast('Profile updated successfully!', 'success');
      } catch (error) {
        console.error('Error updating profile:', error);
        showToast('Failed to update profile', 'error');
      } finally {
        submitBtn.disabled = false;
        submitBtn.innerHTML = '<i class="ph-duotone ph-floppy-disk"></i> Save Changes';
      }
    });

    // Change password
    document.getElementById('passwordForm').addEventListener('submit', async (e) => {
      e.preventDefault();

      const newPassword = document.getElementById('newPassword').value;
      const confirmPassword = document.getElementById('confirmPassword').value;

      if (newPassword !== confirmPassword) {
        showToast('Passwords do not match', 'error');
        return;
      }

      const submitBtn = e.target.querySelector('button[type="submit"]');
      submitBtn.disabled = true;
      submitBtn.innerHTML = '<span class="loading"></span> Updating...';

      try {
        const { error } = await supabase.auth.updateUser({
          password: newPassword
        });

        if (error) throw error;

        showToast('Password updated successfully!', 'success');
        document.getElementById('passwordForm').reset();
      } catch (error) {
        console.error('Error updating password:', error);
        showToast(error.message || 'Failed to update password', 'error');
      } finally {
        submitBtn.disabled = false;
        submitBtn.innerHTML = '<i class="ph-duotone ph-key"></i> Update Password';
      }
    });

    // Delete account
    async function confirmDeleteAccount() {
      const confirmed = confirm('Are you sure you want to delete your account? This action cannot be undone.');
      
      if (!confirmed) return;

      const user = await getCurrentUser();
      if (!user) return;

      try {
        // Delete user data from users table
        await supabase.from('users').delete().eq('id', user.id);
        
        // Sign out
        await supabase.auth.signOut();
        
        showToast('Account deleted. Redirecting...', 'success');
        setTimeout(() => {
          window.location.href = '/index.html';
        }, 1500);
      } catch (error) {
        console.error('Error deleting account:', error);
        showToast('Failed to delete account. Please contact support.', 'error');
      }
    }
  </script>

  <style>
    @media (max-width: 900px) {
      .dashboard-container > div[style*="grid-template-columns: 1fr 1fr"] {
        grid-template-columns: 1fr !important;
      }
    }
  </style>

</body>
</html>
</file>

<file path="supabase/schema.sql">
-- ================================================
-- MAID TO CLEAN DATABASE SCHEMA
-- Run this in Supabase SQL Editor
-- ================================================

-- Users table (extends Supabase auth.users)
CREATE TABLE IF NOT EXISTS users (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  email TEXT,
  full_name TEXT,
  phone TEXT,
  address TEXT,
  role TEXT NOT NULL DEFAULT 'customer' CHECK (role IN ('admin', 'customer')),
  created_at TIMESTAMP DEFAULT NOW()
);

-- Services table
CREATE TABLE IF NOT EXISTS services (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  description TEXT,
  base_price NUMERIC NOT NULL,
  duration_hours NUMERIC DEFAULT 2,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Add-ons table
CREATE TABLE IF NOT EXISTS add_ons (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  price NUMERIC NOT NULL,
  description TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Appointments table
CREATE TABLE IF NOT EXISTS appointments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  service_id UUID REFERENCES services(id),
  rooms INTEGER DEFAULT 1,
  square_footage INTEGER,
  appointment_date DATE NOT NULL,
  appointment_time TIME NOT NULL,
  notes TEXT,
  custom_details JSONB,
  add_ons TEXT[],
  quoted_price NUMERIC,
  status TEXT DEFAULT 'pending' CHECK (status IN ('pending', 'approved', 'paid', 'completed', 'cancelled')),
  created_at TIMESTAMP DEFAULT NOW()
);

-- Payments table
CREATE TABLE IF NOT EXISTS payments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  appointment_id UUID REFERENCES appointments(id) ON DELETE CASCADE,
  amount NUMERIC NOT NULL,
  status TEXT DEFAULT 'pending' CHECK (status IN ('pending', 'paid', 'refunded', 'failed')),
  stripe_session_id TEXT,
  stripe_payment_intent TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Messages table
CREATE TABLE IF NOT EXISTS messages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  sender_id UUID REFERENCES users(id) ON DELETE CASCADE,
  receiver_id UUID REFERENCES users(id) ON DELETE CASCADE,
  content TEXT NOT NULL,
  read BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Indexes for performance
CREATE INDEX IF NOT EXISTS idx_appointments_user ON appointments(user_id);
CREATE INDEX IF NOT EXISTS idx_appointments_date ON appointments(appointment_date);
CREATE INDEX IF NOT EXISTS idx_appointments_status ON appointments(status);
CREATE INDEX IF NOT EXISTS idx_payments_user ON payments(user_id);
CREATE INDEX IF NOT EXISTS idx_payments_status ON payments(status);
CREATE INDEX IF NOT EXISTS idx_messages_receiver ON messages(receiver_id);

-- ================================================
-- ROW LEVEL SECURITY (RLS) POLICIES
-- ================================================

-- Enable RLS on all tables
ALTER TABLE users ENABLE ROW LEVEL SECURITY;
ALTER TABLE services ENABLE ROW LEVEL SECURITY;
ALTER TABLE add_ons ENABLE ROW LEVEL SECURITY;
ALTER TABLE appointments ENABLE ROW LEVEL SECURITY;
ALTER TABLE payments ENABLE ROW LEVEL SECURITY;
ALTER TABLE messages ENABLE ROW LEVEL SECURITY;

-- Users policies
CREATE POLICY "Users can view their own profile" ON users
  FOR SELECT USING (auth.uid() = id);

CREATE POLICY "Users can update their own profile" ON users
  FOR UPDATE USING (auth.uid() = id);

CREATE POLICY "Enable insert for authenticated users" ON users
  FOR INSERT WITH CHECK (auth.uid() = id);

CREATE POLICY "Admins can view all users" ON users
  FOR SELECT USING (
    EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'admin')
  );

-- Services (public read)
CREATE POLICY "Anyone can view services" ON services
  FOR SELECT TO PUBLIC USING (true);

-- Add-ons (public read)
CREATE POLICY "Anyone can view add-ons" ON add_ons
  FOR SELECT TO PUBLIC USING (true);

-- Appointments policies
CREATE POLICY "Users can view their own appointments" ON appointments
  FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can create appointments" ON appointments
  FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Admins can view all appointments" ON appointments
  FOR SELECT USING (
    EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'admin')
  );

CREATE POLICY "Admins can update appointments" ON appointments
  FOR UPDATE USING (
    EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'admin')
  );

-- Payments policies
CREATE POLICY "Users can view their own payments" ON payments
  FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Admins can view all payments" ON payments
  FOR SELECT USING (
    EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'admin')
  );

-- Messages policies
CREATE POLICY "Users can view their own messages" ON messages
  FOR SELECT USING (auth.uid() = receiver_id OR auth.uid() = sender_id);

CREATE POLICY "Users can send messages" ON messages
  FOR INSERT WITH CHECK (auth.uid() = sender_id);

CREATE POLICY "Admins can view all messages" ON messages
  FOR SELECT USING (
    EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'admin')
  );

CREATE POLICY "Admins can send messages" ON messages
  FOR INSERT WITH CHECK (
    EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'admin')
  );
</file>

<file path="supabase/seed.sql">
-- ================================================
-- MAID TO CLEAN SEED DATA
-- Run after schema.sql
-- ================================================

-- Insert Services
INSERT INTO services (name, description, base_price, duration_hours) VALUES
  ('Deep Cleaning', 'Comprehensive cleaning that reaches every corner, leaving your space immaculately refreshed from top to bottom.', 150.00, 4),
  ('Move In/Out Cleaning', 'Perfect for transitions. Whether moving into a new home or preparing to hand over keys, we ensure every space is spotless.', 200.00, 5),
  ('Regular Maintenance', 'Keep your space consistently fresh with scheduled cleaning services. Available weekly, bi-weekly, or monthly.', 100.00, 2),
  ('Custom Cleaning', 'Create your own cleaning package. Select specific rooms, appliances, and tasks—pay only for what you need.', 0.00, 0),
  ('Office Cleaning', 'Professional workspace cleaning to keep your business environment productive, healthy, and impressive.', 125.00, 3),
  ('Post-Construction Cleaning', 'Specialized cleaning after renovations or construction. We remove dust, debris, and residue to reveal your finished space.', 250.00, 6)
ON CONFLICT DO NOTHING;

-- Insert Add-ons
INSERT INTO add_ons (name, price, description) VALUES
  ('Oven Cleaning', 35.00, 'Deep clean inside and outside of oven'),
  ('Refrigerator Cleaning', 40.00, 'Interior and exterior refrigerator cleaning'),
  ('Interior Windows', 30.00, 'Clean all interior windows'),
  ('Laundry Service', 25.00, 'Wash, dry, and fold laundry'),
  ('Cabinet Organization', 45.00, 'Organize and clean inside cabinets'),
  ('Baseboards', 20.00, 'Detailed baseboard cleaning'),
  ('Ceiling Fans', 15.00, 'Dust and clean all ceiling fans'),
  ('Blinds Cleaning', 35.00, 'Deep clean all window blinds')
ON CONFLICT DO NOTHING;
</file>

<file path="public/about.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Learn about Maid To Clean and founder Stephanie Kirkley - our mission, values, and commitment to the Montgomery AL community.">
  <title>About Us | Maid To Clean</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <!-- About Hero -->
  <section class="about-hero">
    <div class="container">
      <h1>About Maid To Clean</h1>
      <p class="tagline">"Polishing Homes, Empowering Communities, and Building Relationships That Last."</p>
    </div>
  </section>

  <!-- Meet Stephanie -->
  <section style="background: var(--background);">
    <div class="container">
      <div style="display: grid; grid-template-columns: 1fr 1.5fr; gap: 4rem; align-items: center;">
        <div>
          <img src="/assets/images/profilepic.webp" alt="Stephanie Kirkley - Owner of Maid To Clean" style="border-radius: var(--radius-lg); width: 100%; box-shadow: 0 20px 40px rgba(0,0,0,0.1);">
        </div>
        <div>
          <h2>Meet Stephanie Kirkley</h2>
          <p class="mb-4" style="font-size: 1.125rem;">Founder & Owner of Maid To Clean</p>
          <p class="mb-4">Maid To Clean was born from a simple belief: every home deserves care, and every community deserves support. What started as one person's determination to provide honest, thorough cleaning services has grown into a trusted team dedicated to transforming spaces and building lasting relationships.</p>
          <p class="mb-4">Stephanie began this journey with nothing but grit, attention to detail, and an unwavering commitment to doing things right. That spirit—what we call the "Stephanie Standard"—still guides everything we do today.</p>
          <p>Based in Montgomery, Alabama, we proudly serve families and businesses throughout the River Region, treating every home as if it were our own.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Core Values -->
  <section class="core-values">
    <div class="container">
      <h2>Our Core Values</h2>
      <p class="intro">These serve as the "Maid To Clean" promise—the principles that guide every visit, every interaction, and every decision we make.</p>
      
      <div class="grid grid-4">
        <div class="value-card">
          <div class="icon"><i class="ph-duotone ph-heart"></i></div>
          <h3>People Over Profit</h3>
          <p>We measure our success by the strength of our relationships and the smiles on our clients' faces, not just the numbers on a ledger.</p>
        </div>
        
        <div class="value-card">
          <div class="icon"><i class="ph-duotone ph-shield-check"></i></div>
          <h3>Uncompromising Integrity</h3>
          <p>We treat every home and office with the utmost respect, ensuring that honesty and transparency are at the heart of every visit.</p>
        </div>
        
        <div class="value-card">
          <div class="icon"><i class="ph-duotone ph-users-three"></i></div>
          <h3>Community-Centric Service</h3>
          <p>We don't just work here; we live here. We are dedicated to lifting up our neighbors and investing in the local community that supports us.</p>
        </div>
        
        <div class="value-card">
          <div class="icon"><i class="ph-duotone ph-star-four"></i></div>
          <h3>The "Stephanie Standard"</h3>
          <p>Inspired by our founder's beginnings, we approach every task with the same grit, detail, and dedication that started it all.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Service Area -->
  <section style="background: var(--surface);">
    <div class="container text-center">
      <h2>Proudly Serving the River Region</h2>
      <p style="font-size: 1.125rem; color: var(--secondary); margin-bottom: 1rem;">Montgomery AL • Prattville AL • Wetumpka AL • Hope Hull AL • Tallassee AL</p>
      <p style="color: var(--secondary);">We're your neighbors, and we're here to help.</p>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <h2>Experience the Maid To Clean Difference</h2>
      <p>Join our family of satisfied clients and discover what happens when integrity meets excellence. We're more than a service; we're your neighbors, and we're here to help.</p>
      <a href="/register.html" class="btn btn-primary btn-lg" data-testid="about-cta-btn">
        <i class="ph-duotone ph-handshake"></i>
        Connect With Us Today
      </a>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="footer-grid">
      <div class="footer-col">
        <h4>Maid To Clean</h4>
        <p>Polishing homes, empowering communities, and building relationships that last.</p>
      </div>
      <div class="footer-col">
        <h4>Quick Links</h4>
        <a href="/about.html">About Us</a>
        <a href="/services.html">Services</a>
        <a href="/contact.html">Contact</a>
      </div>
      <div class="footer-col">
        <h4>Service Areas</h4>
        <p>Montgomery AL</p>
        <p>Prattville AL</p>
        <p>Wetumpka AL</p>
        <p>Hope Hull AL</p>
        <p>Tallassee AL</p>
      </div>
      <div class="footer-col">
        <h4>Contact</h4>
        <p><a href="tel:+13344254566">(334) 425-4566</a></p>
        <p><a href="mailto:stephanie.maidtoclean@gmail.com">stephanie.maidtoclean@gmail.com</a></p>
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; 2026 Maid To Clean. All rights reserved.</p>
    </div>
  </footer>

  <style>
    @media (max-width: 768px) {
      section > .container > div[style*="grid-template-columns: 1fr 1.5fr"] {
        grid-template-columns: 1fr !important;
        gap: 2rem !important;
      }
    }
  </style>

</body>
</html>
</file>

<file path="public/admin/admin-dashboard.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard | Maid To Clean</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/admin.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/dashboard-protect.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <main class="admin-container">
    <div class="admin-header">
      <h1>Admin Dashboard</h1>
      <a href="/admin/admin-customers.html" class="btn btn-secondary btn-sm">
        <i class="ph-duotone ph-users"></i> View Customers
      </a>
    </div>

    <!-- Stats Grid -->
    <div class="admin-stats-grid">
      <div class="admin-stat-card">
        <h4>Total Customers</h4>
        <div class="value" id="totalCustomers">0</div>
      </div>
      <div class="admin-stat-card">
        <h4>Pending Bookings</h4>
        <div class="value" id="pendingBookings">0</div>
      </div>
      <div class="admin-stat-card">
        <h4>This Month Revenue</h4>
        <div class="value" id="monthRevenue">$0</div>
      </div>
      <div class="admin-stat-card">
        <h4>Total Revenue</h4>
        <div class="value" id="totalRevenue">$0</div>
      </div>
    </div>

    <!-- Recent Bookings -->
    <div class="admin-card">
      <div class="admin-card-header">
        <h3><i class="ph-duotone ph-calendar-check"></i> Recent Booking Requests</h3>
        <a href="/admin/admin-customers.html" class="btn btn-secondary btn-sm">View All</a>
      </div>
      <div class="admin-card-body">
        <div id="recentBookings" style="overflow-x: auto;">
          <div class="empty-state">
            <i class="ph-duotone ph-calendar-blank"></i>
            <p>No pending bookings</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Quick Actions -->
    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; margin-top: 1.5rem;">
      <div class="admin-card">
        <div class="admin-card-header">
          <h3><i class="ph-duotone ph-lightning"></i> Quick Actions</h3>
        </div>
        <div class="admin-card-body">
          <div style="display: flex; flex-direction: column; gap: 0.75rem;">
            <a href="/admin/admin-customers.html" class="btn btn-secondary">
              <i class="ph-duotone ph-users"></i> Manage Customers
            </a>
            <a href="/admin/admin-messages.html" class="btn btn-secondary">
              <i class="ph-duotone ph-envelope"></i> Send Message
            </a>
            <a href="/admin/admin-billing.html" class="btn btn-secondary">
              <i class="ph-duotone ph-chart-line-up"></i> View Billing
            </a>
          </div>
        </div>
      </div>

      <div class="admin-card">
        <div class="admin-card-header">
          <h3><i class="ph-duotone ph-megaphone"></i> Broadcast Message</h3>
        </div>
        <div class="admin-card-body">
          <form id="broadcastForm">
            <div class="form-group">
              <textarea id="broadcastMessage" placeholder="Type a message to send to all customers..." style="min-height: 100px;"></textarea>
            </div>
            <button type="submit" class="btn btn-primary w-full" data-testid="send-broadcast-btn">
              <i class="ph-duotone ph-paper-plane-tilt"></i> Send to All
            </button>
          </form>
        </div>
      </div>
    </div>
  </main>

  <!-- Toast Container -->
  <div id="toastContainer" class="toast-container"></div>

  <script>
    function showToast(message, type = 'info') {
      const container = document.getElementById('toastContainer');
      const toast = document.createElement('div');
      toast.className = `toast ${type}`;
      toast.innerHTML = `
        <i class="ph-duotone ph-${type === 'success' ? 'check-circle' : type === 'error' ? 'x-circle' : 'info'}"></i>
        <span>${message}</span>
      `;
      container.appendChild(toast);
      setTimeout(() => toast.remove(), 4000);
    }

    document.addEventListener('DOMContentLoaded', async () => {
      await loadAdminDashboard();
    });

    async function loadAdminDashboard() {
      try {
        // Get total customers
        const { count: customerCount } = await supabase
          .from('users')
          .select('*', { count: 'exact', head: true })
          .eq('role', 'customer');

        document.getElementById('totalCustomers').textContent = customerCount || 0;

        // Get pending bookings
        const { data: pendingBookings, count: pendingCount } = await supabase
          .from('appointments')
          .select(`
            *,
            users (email, full_name),
            services (name)
          `, { count: 'exact' })
          .eq('status', 'pending')
          .order('created_at', { ascending: false });

        document.getElementById('pendingBookings').textContent = pendingCount || 0;

        // Show recent bookings
        if (pendingBookings && pendingBookings.length > 0) {
          document.getElementById('recentBookings').innerHTML = `
            <table class="admin-table">
              <thead>
                <tr>
                  <th>Customer</th>
                  <th>Service</th>
                  <th>Date</th>
                  <th>Status</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody>
                ${pendingBookings.slice(0, 5).map(booking => `
                  <tr>
                    <td>
                      <div style="display: flex; align-items: center; gap: 0.75rem;">
                        <div class="customer-avatar">${(booking.users?.full_name || booking.users?.email || 'U').charAt(0).toUpperCase()}</div>
                        <div>
                          <p style="font-weight: 500;">${booking.users?.full_name || 'Unknown'}</p>
                          <small style="color: var(--secondary);">${booking.users?.email || ''}</small>
                        </div>
                      </div>
                    </td>
                    <td>${booking.services?.name || 'Cleaning'}</td>
                    <td>${new Date(booking.appointment_date).toLocaleDateString()}</td>
                    <td><span class="status-badge pending">${booking.status}</span></td>
                    <td>
                      <button class="action-btn view" onclick="approveBooking('${booking.id}')" data-testid="approve-btn-${booking.id}">
                        <i class="ph-duotone ph-check"></i> Approve
                      </button>
                    </td>
                  </tr>
                `).join('')}
              </tbody>
            </table>
          `;
        }

        // Get payments
        const { data: payments } = await supabase
          .from('payments')
          .select('amount, created_at')
          .eq('status', 'paid');

        if (payments) {
          const totalRevenue = payments.reduce((sum, p) => sum + (p.amount || 0), 0);
          document.getElementById('totalRevenue').textContent = `$${totalRevenue.toFixed(2)}`;

          // This month's revenue
          const thisMonth = new Date();
          const monthStart = new Date(thisMonth.getFullYear(), thisMonth.getMonth(), 1).toISOString();
          const monthPayments = payments.filter(p => p.created_at >= monthStart);
          const monthRevenue = monthPayments.reduce((sum, p) => sum + (p.amount || 0), 0);
          document.getElementById('monthRevenue').textContent = `$${monthRevenue.toFixed(2)}`;
        }

      } catch (error) {
        console.error('Error loading dashboard:', error);
        showToast('Failed to load dashboard data', 'error');
      }
    }

    async function approveBooking(bookingId) {
      try {
        // Get the booking details with user and service info
        const { data: booking } = await supabase
          .from('appointments')
          .select('*, services (name, base_price), users (email, full_name)')
          .eq('id', bookingId)
          .single();

        if (!booking) throw new Error('Booking not found');

        // Calculate quoted price (base price for now, can add add-ons later)
        const quotedPrice = booking.services?.base_price || 100;

        // Update appointment status
        const { error } = await supabase
          .from('appointments')
          .update({ 
            status: 'approved',
            quoted_price: quotedPrice
          })
          .eq('id', bookingId);

        if (error) throw error;

        // Send approval email to customer
        try {
          await fetch('/api/notify-booking', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              customerEmail: booking.users?.email,
              customerName: booking.users?.full_name || '',
              serviceName: booking.services?.name || 'Cleaning Service',
              appointmentDate: new Date(booking.appointment_date).toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric', year: 'numeric' }),
              appointmentTime: booking.appointment_time,
              type: 'approved'
            })
          });
        } catch (emailErr) {
          console.error('Approval email failed:', emailErr);
        }

        showToast('Booking approved! Customer can now pay.', 'success');
        await loadAdminDashboard();
      } catch (error) {
        console.error('Error approving booking:', error);
        showToast('Failed to approve booking', 'error');
      }
    }

    // Broadcast message
    document.getElementById('broadcastForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const message = document.getElementById('broadcastMessage').value.trim();
      if (!message) {
        showToast('Please enter a message', 'error');
        return;
      }

      const submitBtn = e.target.querySelector('button[type="submit"]');
      submitBtn.disabled = true;
      submitBtn.innerHTML = '<span class="loading"></span> Sending...';

      try {
        const admin = await getCurrentUser();
        
        // Get all customers
        const { data: customers } = await supabase
          .from('users')
          .select('id')
          .eq('role', 'customer');

        if (customers && customers.length > 0) {
          // Insert message for each customer
          const messages = customers.map(c => ({
            sender_id: admin.id,
            receiver_id: c.id,
            content: message
          }));

          const { error } = await supabase.from('messages').insert(messages);
          if (error) throw error;

          showToast(`Message sent to ${customers.length} customers!`, 'success');
          document.getElementById('broadcastMessage').value = '';
        } else {
          showToast('No customers to send message to', 'info');
        }
      } catch (error) {
        console.error('Error sending broadcast:', error);
        showToast('Failed to send message', 'error');
      } finally {
        submitBtn.disabled = false;
        submitBtn.innerHTML = '<i class="ph-duotone ph-paper-plane-tilt"></i> Send to All';
      }
    });
  </script>

  <style>
    @media (max-width: 900px) {
      .admin-container > div[style*="grid-template-columns: 1fr 1fr"] {
        grid-template-columns: 1fr !important;
      }
    }
  </style>

</body>
</html>
</file>

<file path="public/assets/js/api-config.js">
// ================================
// SUPABASE CONFIGURATION
// ================================
const SUPABASE_URL = "https://lihyhtcjrmkivmbvgnqp.supabase.co";
const SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImxpaHlodGNqcm1raXZtYnZnbnFwIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzIzMzQyNTIsImV4cCI6MjA4NzkxMDI1Mn0.vMGlCujWsQp6d4tfFX3hLI6IxAf4OIj4hciuMFfw2q8";

const supabase = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);

// ================================
// STRIPE CONFIGURATION
// ================================
const STRIPE_PUBLIC_KEY = "pk_test_51T6671Gq9xS4z8Cj6q0NXf3PZZYHSF330e2XE8G8l0CEKhgkCujxUOD1slGiKH7BY5LtAZecT5CyPg0l1B2qqGI600zNfFw2PT";

// Initialize Stripe only if the library is loaded
let stripe = null;
if (typeof Stripe !== 'undefined') {
  stripe = Stripe(STRIPE_PUBLIC_KEY);
}
</file>

<file path="public/components/navbar.html">
<nav class="navbar" id="mainNav">
  <div class="nav-container">
    <a href="/index.html" class="logo" data-testid="logo-link">
      <img src="/assets/images/new-logo.webp" alt="Maid To Clean Logo" class="logo-img">
      <span class="logo-text">Maid To Clean</span>
    </a>

    <button class="hamburger" id="hamburger" aria-label="Toggle menu" aria-expanded="false" data-testid="hamburger-btn">
      <span></span>
      <span></span>
      <span></span>
    </button>

    <ul class="nav-links" id="nav-links">
      <li><a href="/index.html">Home</a></li>
      <li><a href="/about.html">About</a></li>
      <li><a href="/services.html">Services</a></li>
      <li><a href="/contact.html">Contact</a></li>

      <li class="guest-only"><a href="/login.html" data-testid="nav-login-link">Login</a></li>
      <li class="guest-only nav-cta"><a href="/register.html" data-testid="nav-register-link">Book Now</a></li>

      <li class="customer-only"><a href="/dashboard/dashboard.html" data-testid="nav-dashboard-link">
        <i class="ph-duotone ph-house-line"></i> Dashboard
      </a></li>

      <li class="admin-only"><a href="/admin/admin-dashboard.html" data-testid="nav-admin-link">
        <i class="ph-duotone ph-gear-six"></i> Admin
      </a></li>

      <li class="auth-only"><a href="#" id="logoutBtn" data-testid="nav-logout-btn">
        <i class="ph-duotone ph-sign-out"></i> Logout
      </a></li>
    </ul>
  </div>
</nav>
</file>

<file path="public/contact.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Contact Maid To Clean - Get in touch for quotes and scheduling. Serving Montgomery AL and the River Region.">
  <title>Contact Us | Maid To Clean</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <!-- Contact Hero -->
  <section class="contact-hero">
    <div class="container">
      <h1>Get In Touch</h1>
      <p>Have questions? Need a quote? We'd love to hear from you.</p>
    </div>
  </section>

  <!-- Contact Section -->
  <section class="contact-section">
    <div class="container">
      <div class="contact-grid">
        <!-- Contact Form -->
        <div class="contact-form-container">
          <h3>Send Us a Message</h3>
          <form id="contactForm">
            <div class="form-group">
              <label for="contactName">Your Name</label>
              <input type="text" id="contactName" name="name" placeholder="John Doe" required data-testid="contact-name-input">
            </div>
            <div class="form-group">
              <label for="contactEmail">Email Address</label>
              <input type="email" id="contactEmail" name="email" placeholder="john@example.com" required data-testid="contact-email-input">
            </div>
            <div class="form-group">
              <label for="contactPhone">Phone Number</label>
              <input type="tel" id="contactPhone" name="phone" placeholder="(334) 123-4567" data-testid="contact-phone-input">
            </div>
            <div class="form-group">
              <label for="contactSubject">Subject</label>
              <select id="contactSubject" name="subject" required data-testid="contact-subject-select">
                <option value="">Select a subject...</option>
                <option value="quote">Get a Quote</option>
                <option value="booking">Booking Question</option>
                <option value="feedback">Feedback</option>
                <option value="other">Other</option>
              </select>
            </div>
            <div class="form-group">
              <label for="contactMessage">Message</label>
              <textarea id="contactMessage" name="message" placeholder="Tell us how we can help..." required data-testid="contact-message-input"></textarea>
            </div>
            <button type="submit" class="btn btn-primary w-full" data-testid="contact-submit-btn">
              <i class="ph-duotone ph-paper-plane-tilt"></i>
              Send Message
            </button>
          </form>
        </div>

        <!-- Contact Info -->
        <div class="contact-info">
          <h3>Contact Information</h3>
          
          <div class="contact-info-item">
            <div class="icon"><i class="ph-duotone ph-phone"></i></div>
            <div>
              <h4>Phone</h4>
              <a href="tel:+13344254566">(334) 425-4566</a>
            </div>
          </div>

          <div class="contact-info-item">
            <div class="icon"><i class="ph-duotone ph-envelope"></i></div>
            <div>
              <h4>Email</h4>
              <a href="mailto:stephanie.maidtoclean@gmail.com">stephanie.maidtoclean@gmail.com</a>
            </div>
          </div>

          <div class="contact-info-item">
            <div class="icon"><i class="ph-duotone ph-clock"></i></div>
            <div>
              <h4>Business Hours</h4>
              <p>Monday - Friday: 8am - 6pm</p>
              <p>Saturday: 9am - 4pm</p>
              <p>Sunday: Closed</p>
            </div>
          </div>

          <div class="contact-info-item">
            <div class="icon"><i class="ph-duotone ph-map-pin"></i></div>
            <div>
              <h4>Service Area</h4>
              <p>Proudly serving the River Region:</p>
              <p style="margin-top: 0.5rem;">Montgomery AL • Prattville AL • Wetumpka AL • Hope Hull AL • Tallassee AL</p>
            </div>
          </div>

          <!-- Quick Actions -->
          <div class="mt-8">
            <h4 class="mb-4">Quick Actions</h4>
            <div class="flex flex-col gap-3">
              <a href="/register.html" class="btn btn-primary" data-testid="contact-book-btn">
                <i class="ph-duotone ph-calendar-check"></i>
                Book a Service
              </a>
              <a href="/services.html" class="btn btn-secondary" data-testid="contact-services-btn">
                <i class="ph-duotone ph-sparkle"></i>
                View Services
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="footer-grid">
      <div class="footer-col">
        <h4>Maid To Clean</h4>
        <p>Polishing homes, empowering communities, and building relationships that last.</p>
      </div>
      <div class="footer-col">
        <h4>Quick Links</h4>
        <a href="/about.html">About Us</a>
        <a href="/services.html">Services</a>
        <a href="/contact.html">Contact</a>
      </div>
      <div class="footer-col">
        <h4>Service Areas</h4>
        <p>Montgomery AL</p>
        <p>Prattville AL</p>
        <p>Wetumpka AL</p>
        <p>Hope Hull AL</p>
        <p>Tallassee AL</p>
      </div>
      <div class="footer-col">
        <h4>Contact</h4>
        <p><a href="tel:+13344254566">(334) 425-4566</a></p>
        <p><a href="mailto:stephanie.maidtoclean@gmail.com">stephanie.maidtoclean@gmail.com</a></p>
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; 2026 Maid To Clean. All rights reserved.</p>
    </div>
  </footer>

  <script>
    document.getElementById('contactForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const formData = {
        name: document.getElementById('contactName').value,
        email: document.getElementById('contactEmail').value,
        phone: document.getElementById('contactPhone').value,
        subject: document.getElementById('contactSubject').value,
        message: document.getElementById('contactMessage').value
      };

      alert('Thank you for your message! We will get back to you soon.');
      e.target.reset();
    });
  </script>

</body>
</html>
</file>

<file path="public/dashboard/schedule.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Schedule Cleaning | Maid To Clean</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/dashboard.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/dashboard-protect.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <main class="dashboard-container">
    <div class="dashboard-header">
      <h1>Schedule Cleaning</h1>
      <p>Book your next cleaning service</p>
    </div>

    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 2rem;">
      <!-- Booking Form -->
      <div class="schedule-form">
        <form id="scheduleForm">
          <div class="form-group">
            <label for="serviceSelect">Select Service</label>
            <select id="serviceSelect" required data-testid="service-select">
              <option value="">Choose a service...</option>
            </select>
          </div>

          <div class="form-group">
            <label for="roomsSelect">Number of Rooms</label>
            <select id="roomsSelect" required data-testid="rooms-select">
              <option value="1">1-2 Rooms</option>
              <option value="2">3-4 Rooms</option>
              <option value="3">5-6 Rooms</option>
              <option value="4">7-8 Rooms</option>
              <option value="5">9+ Rooms</option>
            </select>
          </div>

          <div class="form-group">
            <label for="squareFootage">Square Footage (optional)</label>
            <input type="number" id="squareFootage" min="0" placeholder="e.g., 1500" data-testid="sqft-input">
          </div>

          <div class="form-group">
            <label for="addonSelect">Add-Ons (hold Ctrl/Cmd to select multiple)</label>
            <select id="addonSelect" multiple data-testid="addon-select"></select>
          </div>

          <!-- Custom Options (shown for Custom Cleaning) -->
          <div id="customOptions" style="display: none;">
            <h4 class="mb-4">Custom Cleaning Options</h4>
            
            <fieldset>
              <legend>Rooms to Clean</legend>
              <label><input type="checkbox" name="rooms" value="bedroom"> Bedroom</label>
              <label><input type="checkbox" name="rooms" value="bathroom"> Bathroom</label>
              <label><input type="checkbox" name="rooms" value="kitchen"> Kitchen</label>
              <label><input type="checkbox" name="rooms" value="living_room"> Living Room</label>
              <label><input type="checkbox" name="rooms" value="office"> Office</label>
              <label><input type="checkbox" name="rooms" value="basement"> Basement</label>
            </fieldset>

            <fieldset>
              <legend>Appliances</legend>
              <label><input type="checkbox" id="allAppliances" value="all"> All Appliances</label>
              <label><input type="checkbox" name="appliances" value="fridge"> Refrigerator</label>
              <label><input type="checkbox" name="appliances" value="oven"> Oven</label>
              <label><input type="checkbox" name="appliances" value="microwave"> Microwave</label>
            </fieldset>

            <fieldset>
              <legend>Additional Options</legend>
              <label><input type="checkbox" name="windows" value="all"> All Windows</label>
              <label><input type="checkbox" name="baseboards" value="baseboards"> Baseboards</label>
              <label><input type="checkbox" name="laundry" value="laundry"> Laundry Service</label>
            </fieldset>
          </div>

          <div class="form-group">
            <label for="appointmentDate">Preferred Date</label>
            <input type="date" id="appointmentDate" required data-testid="date-input">
          </div>

          <div class="form-group">
            <label for="timeSlots">Available Time Slots</label>
            <select id="timeSlots" required data-testid="time-select">
              <option value="">Select a date first...</option>
            </select>
          </div>

          <div class="form-group">
            <label for="notes">Special Instructions</label>
            <textarea id="notes" placeholder="Any specific requests or notes for our cleaners..." data-testid="notes-input"></textarea>
          </div>

          <button type="submit" class="btn btn-primary w-full" data-testid="submit-booking-btn">
            <i class="ph-duotone ph-paper-plane-tilt"></i>
            Submit Booking Request
          </button>
        </form>
      </div>

      <!-- Past Appointments -->
      <div>
        <div class="dashboard-card">
          <h3><i class="ph-duotone ph-clock-counter-clockwise"></i> Your Appointments</h3>
          <div id="appointmentHistory">
            <div class="empty-state">
              <i class="ph-duotone ph-calendar-blank"></i>
              <p>No appointments yet</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>

  <!-- Toast Container -->
  <div id="toastContainer" class="toast-container"></div>

  <script>
    const serviceSelect = document.getElementById('serviceSelect');
    const customOptions = document.getElementById('customOptions');
    const appointmentDate = document.getElementById('appointmentDate');
    const timeSlots = document.getElementById('timeSlots');
    const scheduleForm = document.getElementById('scheduleForm');

    // Set minimum date to today
    const today = new Date().toISOString().split('T')[0];
    appointmentDate.min = today;

    function showToast(message, type = 'info') {
      const container = document.getElementById('toastContainer');
      const toast = document.createElement('div');
      toast.className = `toast ${type}`;
      toast.innerHTML = `
        <i class="ph-duotone ph-${type === 'success' ? 'check-circle' : type === 'error' ? 'x-circle' : 'info'}"></i>
        <span>${message}</span>
      `;
      container.appendChild(toast);
      setTimeout(() => toast.remove(), 4000);
    }

    // Load services
    async function loadServices() {
      const { data: services, error } = await supabase
        .from('services')
        .select('*')
        .order('created_at', { ascending: true });

      if (error) {
        console.error('Error loading services:', error);
        return;
      }

      services.forEach(s => {
        const option = document.createElement('option');
        option.value = s.id;
        option.textContent = `${s.name} - From $${s.base_price}`;
        option.dataset.name = s.name;
        serviceSelect.appendChild(option);
      });
    }

    // Load add-ons
    async function loadAddOns() {
      const addonSelect = document.getElementById('addonSelect');
      const { data: addOns, error } = await supabase.from('add_ons').select('*');

      if (error) {
        console.error('Error loading add-ons:', error);
        return;
      }

      addOns.forEach(a => {
        const option = document.createElement('option');
        option.value = a.id;
        option.textContent = `${a.name} (+$${a.price})`;
        addonSelect.appendChild(option);
      });
    }

    // Show custom options for Custom Cleaning
    serviceSelect.addEventListener('change', (e) => {
      const selectedOption = serviceSelect.options[serviceSelect.selectedIndex];
      const serviceName = selectedOption.dataset.name;
      customOptions.style.display = serviceName === 'Custom Cleaning' ? 'block' : 'none';
    });

    // Load available time slots
    async function loadTimeSlots(date) {
      timeSlots.innerHTML = '<option value="">Loading...</option>';

      if (!date) {
        timeSlots.innerHTML = '<option value="">Select a date first...</option>';
        return;
      }

      const slots = ['08:00', '09:00', '10:00', '11:00', '12:00', '13:00', '14:00', '15:00', '16:00', '17:00'];

      // Get booked slots
      const { data: booked, error } = await supabase
        .from('appointments')
        .select('appointment_time')
        .eq('appointment_date', date)
        .not('status', 'eq', 'cancelled');

      if (error) {
        console.error('Error loading slots:', error);
        timeSlots.innerHTML = '<option value="">Error loading slots</option>';
        return;
      }

      const bookedTimes = booked ? booked.map(b => b.appointment_time.substring(0, 5)) : [];
      const availableSlots = slots.filter(s => !bookedTimes.includes(s));

      timeSlots.innerHTML = '<option value="">Choose a time...</option>';
      
      if (availableSlots.length === 0) {
        timeSlots.innerHTML = '<option value="">No slots available</option>';
        return;
      }

      availableSlots.forEach(s => {
        const option = document.createElement('option');
        option.value = s;
        const hour = parseInt(s.split(':')[0]);
        const period = hour >= 12 ? 'PM' : 'AM';
        const displayHour = hour > 12 ? hour - 12 : hour;
        option.textContent = `${displayHour}:00 ${period}`;
        timeSlots.appendChild(option);
      });
    }

    appointmentDate.addEventListener('change', (e) => {
      loadTimeSlots(e.target.value);
    });

    // Load past appointments
    async function loadAppointmentHistory() {
      const user = await getCurrentUser();
      if (!user) return;

      const { data: appointments, error } = await supabase
        .from('appointments')
        .select(`
          *,
          services (name)
        `)
        .eq('user_id', user.id)
        .order('appointment_date', { ascending: false })
        .limit(10);

      const container = document.getElementById('appointmentHistory');

      if (error || !appointments || appointments.length === 0) {
        return;
      }

      container.innerHTML = appointments.map(apt => `
        <div style="padding: 1rem; background: var(--background); border-radius: var(--radius-md); margin-bottom: 0.75rem; border-left: 3px solid ${apt.status === 'paid' || apt.status === 'completed' ? 'var(--success)' : apt.status === 'approved' ? 'var(--accent)' : 'var(--border)'};">
          <div style="display: flex; justify-content: space-between; align-items: start;">
            <div>
              <p style="font-weight: 600;">${apt.services?.name || 'Cleaning Service'}</p>
              <p style="font-size: 0.875rem; color: var(--secondary);">
                ${new Date(apt.appointment_date).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })} at ${apt.appointment_time}
              </p>
            </div>
            <span class="status-badge ${apt.status}">${apt.status}</span>
          </div>
        </div>
      `).join('');
    }

    // Submit booking
    scheduleForm.addEventListener('submit', async (e) => {
      e.preventDefault();

      const user = await getCurrentUser();
      if (!user) {
        showToast('Please login to book a service', 'error');
        return;
      }

      const submitBtn = scheduleForm.querySelector('button[type="submit"]');
      submitBtn.disabled = true;
      submitBtn.innerHTML = '<span class="loading"></span> Submitting...';

      try {
        const serviceId = serviceSelect.value;
        const rooms = parseInt(document.getElementById('roomsSelect').value);
        const squareFootage = parseInt(document.getElementById('squareFootage').value) || null;
        const date = appointmentDate.value;
        const time = timeSlots.value;
        const notes = document.getElementById('notes').value;

        // Collect add-ons
        const addonSelect = document.getElementById('addonSelect');
        const selectedAddOns = Array.from(addonSelect.selectedOptions).map(opt => opt.value);

        // Collect custom details if applicable
        let customDetails = null;
        if (customOptions.style.display === 'block') {
          customDetails = {
            rooms: Array.from(document.querySelectorAll('input[name="rooms"]:checked')).map(cb => cb.value),
            appliances: document.getElementById('allAppliances').checked 
              ? 'all' 
              : Array.from(document.querySelectorAll('input[name="appliances"]:checked')).map(cb => cb.value),
            windows: document.querySelector('input[name="windows"]')?.checked ? 'all' : null,
            baseboards: document.querySelector('input[name="baseboards"]')?.checked || false,
            laundry: document.querySelector('input[name="laundry"]')?.checked || false
          };
        }

        // Get service name for email
        const selectedOption = serviceSelect.options[serviceSelect.selectedIndex];
        const serviceName = selectedOption.dataset.name || 'Cleaning Service';

        const { data, error } = await supabase.from('appointments').insert([{
          user_id: user.id,
          service_id: serviceId,
          rooms: rooms,
          square_footage: squareFootage,
          appointment_date: date,
          appointment_time: time,
          notes: notes,
          custom_details: customDetails,
          add_ons: selectedAddOns,
          status: 'pending'
        }]);

        if (error) throw error;

        // Send email notifications
        try {
          const profile = await getUserProfile();
          await fetch('/api/notify-booking', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              customerEmail: user.email,
              customerName: profile?.full_name || '',
              serviceName: serviceName,
              appointmentDate: new Date(date).toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric', year: 'numeric' }),
              appointmentTime: time,
              notes: notes,
              type: 'new_booking'
            })
          });
        } catch (emailErr) {
          console.error('Email notification failed:', emailErr);
          // Don't block the booking if email fails
        }

        showToast('Booking request submitted! We will review and confirm shortly.', 'success');
        scheduleForm.reset();
        customOptions.style.display = 'none';
        timeSlots.innerHTML = '<option value="">Select a date first...</option>';
        await loadAppointmentHistory();

      } catch (error) {
        console.error('Booking error:', error);
        showToast('Failed to submit booking. Please try again.', 'error');
      } finally {
        submitBtn.disabled = false;
        submitBtn.innerHTML = '<i class="ph-duotone ph-paper-plane-tilt"></i> Submit Booking Request';
      }
    });

    // Initialize
    document.addEventListener('DOMContentLoaded', () => {
      loadServices();
      loadAddOns();
      loadAppointmentHistory();
    });
  </script>

  <style>
    @media (max-width: 900px) {
      .dashboard-container > div[style*="grid-template-columns: 1fr 1fr"] {
        grid-template-columns: 1fr !important;
      }
    }
  </style>

</body>
</html>
</file>

<file path="public/index.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Maid To Clean - Professional cleaning services in Montgomery AL and the River Region. Built on integrity, driven by excellence.">
  <title>Maid To Clean | Professional Cleaning Services | Montgomery AL</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <!-- Hero Section -->
  <section class="hero">
    <img 
      src="/assets/images/livingroom.webp" 
      alt="Clean modern living room" 
      class="hero-bg"
    >
    <div class="hero-content">
      <h1>The Future of Clean</h1>
      <p>Built on integrity, driven by excellence—your trusted partner for professional cleaning services in Montgomery and the River Region.</p>
      <div class="hero-buttons">
        <a href="/register.html" class="btn btn-primary btn-lg" data-testid="hero-book-btn">
          <i class="ph-duotone ph-calendar-check"></i>
          Book Today
        </a>
        <a href="/services.html" class="btn btn-outline btn-lg" data-testid="hero-services-btn">
          <i class="ph-duotone ph-sparkle"></i>
          View Services
        </a>
      </div>
    </div>
  </section>

  <!-- Services Preview -->
  <section class="services-preview">
    <div class="container">
      <h2>Our Services</h2>
      <div class="grid grid-3" id="servicesGrid">
        <!-- Service Card 1 -->
        <div class="service-card">
          <img src="/assets/images/deep-clean.webp" alt="Deep Cleaning Service" class="service-card-image">
          <div class="service-card-content">
            <h3>Deep Cleaning</h3>
            <p>Comprehensive cleaning that reaches every corner, leaving your space immaculately refreshed.</p>
          </div>
        </div>

        <!-- Service Card 2 -->
        <div class="service-card">
          <img src="/assets/images/livingroom.webp" alt="Standard Cleaning" class="service-card-image">
          <div class="service-card-content">
            <h3>Standard Cleaning</h3>
            <p>Regular maintenance cleaning to keep your home fresh and welcoming every day.</p>
          </div>
        </div>

        <!-- Service Card 3 -->
        <div class="service-card">
          <img src="/assets/images/move-in-out.webp" alt="Move In/Out Cleaning" class="service-card-image">
          <div class="service-card-content">
            <h3>Move-In/Out</h3>
            <p>Perfect for transitions—ensure your new home is spotless or leave your old one pristine.</p>
          </div>
        </div>
      </div>
      <div class="text-center mt-8">
        <a href="/services.html" class="btn btn-secondary" data-testid="view-all-services-btn">
          View All Services
          <i class="ph-duotone ph-arrow-right"></i>
        </a>
      </div>
    </div>
  </section>

  <!-- Why Choose Us -->
  <section class="why-us">
    <div class="container">
      <h2>Why Clients Choose Maid To Clean</h2>
      <div class="why-us-grid">
        <div class="why-us-item">
          <div class="icon"><i class="ph-duotone ph-heart"></i></div>
          <h4>People Over Profit</h4>
          <p>We measure success by the strength of our relationships and the smiles on our clients' faces.</p>
        </div>
        <div class="why-us-item">
          <div class="icon"><i class="ph-duotone ph-shield-check"></i></div>
          <h4>Uncompromising Integrity</h4>
          <p>Honesty and transparency are at the heart of every visit. Your trust is our priority.</p>
        </div>
        <div class="why-us-item">
          <div class="icon"><i class="ph-duotone ph-users-three"></i></div>
          <h4>Community-Centric</h4>
          <p>We don't just work here; we live here. Proudly serving Montgomery and the River Region.</p>
        </div>
        <div class="why-us-item">
          <div class="icon"><i class="ph-duotone ph-star-four"></i></div>
          <h4>The Stephanie Standard</h4>
          <p>Every task done with the same grit, detail, and dedication that started it all.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Service Areas -->
  <section style="background: var(--background);">
    <div class="container text-center">
      <h2>Proudly Serving the River Region</h2>
      <p style="font-size: 1.125rem; color: var(--secondary); margin-bottom: 2rem;">Montgomery AL • Prattville AL • Wetumpka AL • Hope Hull AL • Tallassee AL</p>
      <a href="/contact.html" class="btn btn-secondary">
        <i class="ph-duotone ph-map-pin"></i>
        Check Service Area
      </a>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <h2>Experience the Maid To Clean Difference</h2>
      <p>Join our family of satisfied clients and discover what happens when integrity meets excellence. We're more than a service; we're your neighbors, and we're here to help.</p>
      <a href="/register.html" class="btn btn-primary btn-lg" data-testid="cta-register-btn">
        <i class="ph-duotone ph-user-plus"></i>
        Connect With Us Today
      </a>
    </div>
  </section>

  <!-- Contact Preview -->
  <section class="contact-cta">
    <div class="container">
      <h2>Ready to Schedule?</h2>
      <p>
        <i class="ph-duotone ph-phone"></i>
        <a href="tel:+13344254566">(334) 425-4566</a>
      </p>
      <p>
        <i class="ph-duotone ph-envelope"></i>
        <a href="mailto:stephanie.maidtoclean@gmail.com">stephanie.maidtoclean@gmail.com</a>
      </p>
      <div class="mt-6">
        <a href="/contact.html" class="btn btn-secondary" data-testid="contact-btn">
          Get In Touch
          <i class="ph-duotone ph-arrow-right"></i>
        </a>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="footer-grid">
      <div class="footer-col">
        <h4>Maid To Clean</h4>
        <p>Polishing homes, empowering communities, and building relationships that last.</p>
      </div>
      <div class="footer-col">
        <h4>Quick Links</h4>
        <a href="/about.html">About Us</a>
        <a href="/services.html">Services</a>
        <a href="/contact.html">Contact</a>
      </div>
      <div class="footer-col">
        <h4>Service Areas</h4>
        <p>Montgomery AL</p>
        <p>Prattville AL</p>
        <p>Wetumpka AL</p>
        <p>Hope Hull AL</p>
        <p>Tallassee AL</p>
      </div>
      <div class="footer-col">
        <h4>Contact</h4>
        <p><a href="tel:+13344254566">(334) 425-4566</a></p>
        <p><a href="mailto:stephanie.maidtoclean@gmail.com">stephanie.maidtoclean@gmail.com</a></p>
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; 2026 Maid To Clean. All rights reserved.</p>
    </div>
  </footer>

</body>
</html>
</file>

<file path="public/login.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login | Maid To Clean | Montgomery AL</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <main class="auth-container">
    <h1>Welcome Back</h1>
    
    <form id="loginForm">
      <div class="form-group">
        <label for="loginEmail">Email Address</label>
        <input type="email" id="loginEmail" placeholder="you@example.com" required data-testid="login-email-input">
      </div>
      <div class="form-group">
        <label for="loginPassword">Password</label>
        <input type="password" id="loginPassword" placeholder="Enter your password" required data-testid="login-password-input">
      </div>
      <button type="submit" class="btn btn-primary" data-testid="login-submit-btn">
        <i class="ph-duotone ph-sign-in"></i>
        Login
      </button>
    </form>
    
    <div class="auth-link">
      Don't have an account? <a href="/register.html">Create one</a>
    </div>
  </main>

  <!-- Toast Container -->
  <div id="toastContainer" class="toast-container"></div>

  <script>
    function showToast(message, type = 'info') {
      const container = document.getElementById('toastContainer');
      const toast = document.createElement('div');
      toast.className = `toast ${type}`;
      toast.innerHTML = `
        <i class="ph-duotone ph-${type === 'success' ? 'check-circle' : type === 'error' ? 'x-circle' : 'info'}"></i>
        <span>${message}</span>
      `;
      container.appendChild(toast);
      setTimeout(() => toast.remove(), 4000);
    }

    document.getElementById('loginForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const email = document.getElementById('loginEmail').value;
      const password = document.getElementById('loginPassword').value;
      const submitBtn = e.target.querySelector('button[type="submit"]');
      
      submitBtn.disabled = true;
      submitBtn.innerHTML = '<span class="loading"></span> Logging in...';
      
      try {
        await loginUser(email, password);
        showToast('Login successful! Redirecting...', 'success');
        
        // Check role and redirect
        const role = await getUserRole();
        setTimeout(() => {
          if (role === 'admin') {
            window.location.href = '/admin/admin-dashboard.html';
          } else {
            window.location.href = '/dashboard/dashboard.html';
          }
        }, 1000);
      } catch (error) {
        showToast(error.message || 'Login failed. Please try again.', 'error');
        submitBtn.disabled = false;
        submitBtn.innerHTML = '<i class="ph-duotone ph-sign-in"></i> Login';
      }
    });
  </script>

</body>
</html>
</file>

<file path="public/register.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Account | Maid To Clean | Montgomery AL</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <main class="auth-container">
    <h1>Create Account</h1>
    
    <form id="registerForm">
      <div class="form-group">
        <label for="registerName">Full Name</label>
        <input type="text" id="registerName" placeholder="John Doe" required data-testid="register-name-input">
      </div>
      <div class="form-group">
        <label for="registerEmail">Email Address</label>
        <input type="email" id="registerEmail" placeholder="you@example.com" required data-testid="register-email-input">
      </div>
      <div class="form-group">
        <label for="registerPhone">Phone Number</label>
        <input type="tel" id="registerPhone" placeholder="(334) 123-4567" data-testid="register-phone-input">
      </div>
      <div class="form-group">
        <label for="registerPassword">Password</label>
        <input type="password" id="registerPassword" placeholder="Create a password (min. 6 characters)" required minlength="6" data-testid="register-password-input">
      </div>
      <button type="submit" class="btn btn-primary" data-testid="register-submit-btn">
        <i class="ph-duotone ph-user-plus"></i>
        Create Account
      </button>
    </form>
    
    <div class="auth-link">
      Already have an account? <a href="/login.html">Login</a>
    </div>
  </main>

  <!-- Toast Container -->
  <div id="toastContainer" class="toast-container"></div>

  <script>
    function showToast(message, type = 'info') {
      const container = document.getElementById('toastContainer');
      const toast = document.createElement('div');
      toast.className = `toast ${type}`;
      toast.innerHTML = `
        <i class="ph-duotone ph-${type === 'success' ? 'check-circle' : type === 'error' ? 'x-circle' : 'info'}"></i>
        <span>${message}</span>
      `;
      container.appendChild(toast);
      setTimeout(() => toast.remove(), 4000);
    }

    document.getElementById('registerForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const name = document.getElementById('registerName').value;
      const email = document.getElementById('registerEmail').value;
      const phone = document.getElementById('registerPhone').value;
      const password = document.getElementById('registerPassword').value;
      const submitBtn = e.target.querySelector('button[type="submit"]');
      
      submitBtn.disabled = true;
      submitBtn.innerHTML = '<span class="loading"></span> Creating account...';
      
      try {
        // Register with Supabase Auth
        const { data, error } = await supabase.auth.signUp({
          email,
          password,
          options: {
            data: {
              full_name: name,
              phone: phone
            }
          }
        });

        if (error) throw error;

        // Insert into users table
        if (data.user) {
          await supabase.from('users').insert([{
            id: data.user.id,
            email: email,
            full_name: name,
            phone: phone,
            role: 'customer'
          }]);
        }

        showToast('Account created successfully! Please check your email to verify.', 'success');
        setTimeout(() => {
          window.location.href = '/login.html';
        }, 2000);
      } catch (error) {
        showToast(error.message || 'Registration failed. Please try again.', 'error');
        submitBtn.disabled = false;
        submitBtn.innerHTML = '<i class="ph-duotone ph-user-plus"></i> Create Account';
      }
    });
  </script>

</body>
</html>
</file>

<file path="public/services.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Professional cleaning services in Montgomery AL - Deep Cleaning, Standard Cleaning, Move-In/Out, Office, Commercial, Post-Construction and Custom Solutions.">
  <title>Our Services | Maid To Clean</title>

  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/assets/css/navbar.css">
  <link rel="stylesheet" href="/assets/css/responsive.css">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script defer src="/assets/js/api-config.js"></script>
  <script defer src="/assets/js/auth.js"></script>
  <script defer src="/assets/js/load-navbar.js"></script>
</head>
<body>

  <div id="navbar"></div>

  <!-- Services Hero -->
  <section class="services-hero">
    <div class="container">
      <h1>Our Services</h1>
      <p>Professional cleaning solutions tailored to your needs. Serving Montgomery and the River Region.</p>
    </div>
  </section>

  <!-- Services Grid -->
  <section class="services-grid-section">
    <div class="container">
      <h2>Choose Your Service</h2>
      
      <div class="grid grid-3">
        <!-- Deep Cleaning -->
        <div class="service-detail-card">
          <img src="/assets/images/deep-clean.webp" alt="Deep Cleaning">
          <div class="service-detail-card-body">
            <h3>Deep Cleaning</h3>
            <p>Our most thorough cleaning service. We reach every corner, scrub every surface, and leave your home immaculately refreshed from top to bottom.</p>
          </div>
        </div>

        <!-- Standard Cleaning -->
        <div class="service-detail-card">
          <img src="/assets/images/livingroom.webp" alt="Standard Cleaning">
          <div class="service-detail-card-body">
            <h3>Standard Cleaning</h3>
            <p>Regular maintenance cleaning to keep your home consistently fresh and welcoming. Perfect for weekly, bi-weekly, or monthly scheduling.</p>
          </div>
        </div>

        <!-- Move In/Out -->
        <div class="service-detail-card">
          <img src="/assets/images/move-in-out.webp" alt="Move In/Out Cleaning">
          <div class="service-detail-card-body">
            <h3>Move-In/Out Cleaning</h3>
            <p>Perfect for transitions. Whether moving into a new home or preparing to hand over keys, we ensure every space is spotless and ready.</p>
          </div>
        </div>

        <!-- Office Cleaning -->
        <div class="service-detail-card">
          <img src="/assets/images/small-office.webp" alt="Office Cleaning">
          <div class="service-detail-card-body">
            <h3>Office Cleaning</h3>
            <p>Professional workspace cleaning to keep your business environment productive, healthy, and impressive for clients and employees.</p>
          </div>
        </div>

        <!-- Commercial -->
        <div class="service-detail-card">
          <img src="/assets/images/commercial-office.webp" alt="Commercial Cleaning">
          <div class="service-detail-card-body">
            <h3>Commercial Cleaning</h3>
            <p>Comprehensive cleaning solutions for businesses of all sizes. Maintain a pristine environment that reflects your professional standards.</p>
          </div>
        </div>

        <!-- Post-Construction -->
        <div class="service-detail-card">
          <img src="/assets/images/construction-site.webp" alt="Post-Construction Cleaning">
          <div class="service-detail-card-body">
            <h3>Post-Construction</h3>
            <p>Specialized cleaning after renovations or construction. We remove dust, debris, and residue to reveal your beautifully finished space.</p>
          </div>
        </div>

        <!-- Custom Solutions -->
        <div class="service-detail-card">
          <img src="/assets/images/custom-list.webp" alt="Custom Cleaning Solutions">
          <div class="service-detail-card-body">
            <h3>Custom Solutions</h3>
            <p>Create your own cleaning package. Select specific rooms, appliances, and tasks—we'll tailor our service to exactly what you need.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Add-Ons Section -->
  <section style="background: var(--surface);">
    <div class="container">
      <h2 class="text-center mb-6">Popular Add-Ons</h2>
      <div class="grid grid-4">
        <div class="card text-center">
          <div class="card-icon" style="margin: 0 auto 1rem;"><i class="ph-duotone ph-oven"></i></div>
          <h4>Oven Cleaning</h4>
        </div>
        <div class="card text-center">
          <div class="card-icon" style="margin: 0 auto 1rem;"><i class="ph-duotone ph-snowflake"></i></div>
          <h4>Refrigerator</h4>
        </div>
        <div class="card text-center">
          <div class="card-icon" style="margin: 0 auto 1rem;"><i class="ph-duotone ph-window"></i></div>
          <h4>Interior Windows</h4>
        </div>
        <div class="card text-center">
          <div class="card-icon" style="margin: 0 auto 1rem;"><i class="ph-duotone ph-t-shirt"></i></div>
          <h4>Laundry Service</h4>
        </div>
      </div>
    </div>
  </section>

  <!-- Service Area -->
  <section style="background: var(--background);">
    <div class="container text-center">
      <h2>Service Area</h2>
      <p style="font-size: 1.125rem; color: var(--secondary); margin-bottom: 1rem;">Proudly serving the River Region</p>
      <p style="color: var(--secondary);">Montgomery AL • Prattville AL • Wetumpka AL • Hope Hull AL • Tallassee AL</p>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <h2>Ready to Experience the Difference?</h2>
      <p>Book your cleaning service today and see why our clients trust us with their homes and offices.</p>
      <div class="hero-buttons">
        <a href="/register.html" class="btn btn-primary btn-lg" data-testid="services-book-btn">
          <i class="ph-duotone ph-calendar-check"></i>
          Book Now
        </a>
        <a href="/contact.html" class="btn btn-outline btn-lg" data-testid="services-contact-btn">
          <i class="ph-duotone ph-chat-circle-dots"></i>
          Get a Quote
        </a>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="footer-grid">
      <div class="footer-col">
        <h4>Maid To Clean</h4>
        <p>Polishing homes, empowering communities, and building relationships that last.</p>
      </div>
      <div class="footer-col">
        <h4>Quick Links</h4>
        <a href="/about.html">About Us</a>
        <a href="/services.html">Services</a>
        <a href="/contact.html">Contact</a>
      </div>
      <div class="footer-col">
        <h4>Service Areas</h4>
        <p>Montgomery AL</p>
        <p>Prattville AL</p>
        <p>Wetumpka AL</p>
        <p>Hope Hull AL</p>
        <p>Tallassee AL</p>
      </div>
      <div class="footer-col">
        <h4>Contact</h4>
        <p><a href="tel:+13344254566">(334) 425-4566</a></p>
        <p><a href="mailto:stephanie.maidtoclean@gmail.com">stephanie.maidtoclean@gmail.com</a></p>
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; 2026 Maid To Clean. All rights reserved.</p>
    </div>
  </footer>

</body>
</html>
</file>

<file path=".emergent/emergent.yml">
{
  "env_image_name": "fastapi_react_mongo_shadcn_base_image_cloud_arm:release-03032026-1",
  "job_id": "dc1dca29-2d25-4c4f-a807-e61261ab6b81",
  "created_at": "2026-03-06T15:32:56.440829+00:00Z"
}
</file>

<file path=".gitignore">
node_modules/
.env
.vercel
.DS_Store-e 
# Environment files
*.env
*.env.*
-e 
# Environment files
*.env
*.env.*
-e 
# Environment files
*.env
*.env.*
-e 
# Environment files
*.env
*.env.*
-e 
# Environment files
*.env
*.env.*
</file>

</files>
